# =========================
# Kansas Geo Timeline — deps
# =========================
# Notes:
# - Rasterio wheels vendor GDAL; no need to pin GDAL separately.
# - Keep NumPy <2 until the stack is verified on 2.x.
# - macOS/Linux: if building from source, install system gdal/proj first.
#   (Homebrew: brew install gdal proj; Debian/Ubuntu: apt-get install gdal-bin libgdal-dev proj-bin)
# - If you rely on constraints, install with:
#     pip install -r requirements.txt --constraint constraints.txt

################
# Core runtime #
################
requests>=2.32.3
tqdm>=4.66.5
numpy>=1.26.4,<2.0              # rasterio/pyproj compatibility
rasterio>=1.3.10,<1.4           # GDAL-backed raster IO; manylinux/macos wheels available
rio-cogeo>=5.3.3                # COG creation/validation
shapely>=2.0.4                  # geometry ops
pyproj>=3.6.1                   # CRS / reprojection
Pillow>=10.4.0                  # image utilities (thumbnails, tiles)

# STAC + schema (provenance / catalog)
pystac>=1.10.1
pystac-client>=0.7.6
jsonschema>=4.23.0

# Tiny CLI / rich logs (used by scripts/ & CLI)
click>=8.1.7
rich>=13.7.1

#########################
# Optional: web helpers #
#########################
# jinja2>=3.1.4           # generate small static configs (web/app.config.json)
# httpx>=0.27.2           # simple HTTP pulls when not using curl/wget

###############################
# Optional: Earth/KML helpers #
###############################
# simplekml>=1.3.6

########################################
# Optional: tiling / vector tile stack #
########################################
# mercantile>=1.2.1         # tile math
# morecantile>=5.3.0        # custom tiling schemes
# mapbox-vector-tile>=1.2.1 # encode MVTs (module: mapbox_vector_tile)
# rio-tiler>=6.6.1          # read/serve raster tiles; pairs well with COGs
# titiler-core>=0.18.2      # lightweight dynamic tile endpoints (if you host tiles)

################################
# Optional: vector IO / geoops #
################################
# pyogrio>=0.9.0             # fast vector IO (GDAL-backed wheels available)
# geopandas>=1.0.0

#############################
# Optional: dev / CI / QA   #
#############################
# ruff>=0.6.9
# black>=24.8.0
# mypy>=1.11.2
# pytest>=8.3.2
# pytest-cov>=5.0.0
# pre-commit>=3.8.0
# pip-audit>=2.7.3
