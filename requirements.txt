# =========================
# Kansas Geo Timeline — deps
# =========================
# Reproducible install:
#   python -m venv .venv && . .venv/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# Notes:
# - Rasterio wheels vendor GDAL; do NOT pin GDAL separately here.
# - Keep NumPy <2 until the stack is verified on 2.x.
# - If building from source on macOS/Linux, install system gdal/proj first:
#     macOS (brew):   brew install gdal proj
#     Debian/Ubuntu:  sudo apt-get install -y gdal-bin libgdal-dev proj-bin
#
# Python: 3.10+ recommended.

################
# Core runtime #
################
requests>=2.32.3,<3
tqdm>=4.66.5,<5
packaging>=24.1,<25
python-dateutil>=2.9.0.post0,<3

numpy>=1.26.4,<2.0              # rasterio/pyproj compatibility
rasterio>=1.3.10,<1.4           # GDAL-backed raster IO; manylinux/macos wheels
rio-cogeo>=5.3.3,<6             # COG creation/validation
shapely>=2.0.4,<3               # geometry ops
pyproj>=3.6.1,<4                # CRS / reprojection
Pillow>=10.4.0,<11              # thumbnails/tiles for web UI

typing-extensions>=4.12.2; python_version < "3.11"
colorama>=0.4.6; platform_system == "Windows"

###############################
# STAC / schema / validation  #
###############################
pystac>=1.10.1,<2
pystac-client>=0.7.6,<1
jsonschema>=4.23.0,<5
stac-validator>=3.3.1,<4        # provides `stac-validate` CLI

#########################
# Tiny CLI / rich logs  #
#########################
click>=8.1.7,<9
rich>=13.7.1,<14

#########################
# Optional: web helpers #
#########################
# jinja2>=3.1.4,<4            # generate static configs (web/app.config.json)
# httpx>=0.27.2,<0.28         # simple HTTP pulls when not using curl/wget

###############################
# Optional: Earth/KML helpers #
###############################
# simplekml>=1.3.6,<2

########################################
# Optional: tiling / vector tile stack #
########################################
# mercantile>=1.2.1,<2
# morecantile>=5.3.0,<6
# mapbox-vector-tile>=1.2.1,<2
# rio-tiler>=6.6.1,<7
# titiler-core>=0.18.2,<0.19

################################
# Optional: vector IO / geoops #
################################
# pyogrio>=0.9.0,<1
# geopandas>=1.0.0,<1.1

#############################
# Optional: dev / CI / QA   #
#############################
# ruff>=0.6.9,<1
# black>=24.8.0,<25
# mypy>=1.11.2,<2
# pytest>=8.3.2,<9
# pytest-cov>=5.0.0,<6
# pre-commit>=3.8.0,<4
# bandit>=1.7,<2
# pip-audit>=2.7.3,<3
