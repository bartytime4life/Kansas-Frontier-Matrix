# UI Story Node (Minimal) Example

This example provides a tiny story-node payload that can be rendered by a KFM UI component.

## Goal

Render a factual claim with resolvable citations and metadata without external dependencies.
