# Validate and Publish Pipeline Example

This example captures a minimal pipeline run from validation through publish-ready artifacts.

## What it covers

1. Validate a dataset manifest and schema.
2. Produce evidence and run receipt.
3. Emit a promotion manifest in `outputs/`.
