# CI helper tests

Deterministic tests for dependency-bootstrap, reviewer-summary, and
report-normalization helpers under `tools/ci/`. Dependency tests prove finite
profiles, exact lock coverage, SHA-256 hash enforcement, and shell-free command
execution. Rendering tests prove bounded presentation behavior. Neither family
creates policy, evidence, proof, release, or publication authority.
