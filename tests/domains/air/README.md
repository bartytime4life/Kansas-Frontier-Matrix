# tests/domains/air

Draft compatibility README.

This path exists, but the related domain documentation uses `atmosphere` as the preferred segment.

Use `tests/domains/atmosphere/` for implementation work until the path naming decision is resolved.
