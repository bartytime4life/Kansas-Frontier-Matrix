# tests/catalog/fixtures

Static JSON fixtures for `tests/catalog/test_catalog_crosslink.py`.

## Files

- `aligned-decision.json`: aligned decision payload.
- `aligned-record.json`: aligned promotion record payload.
- `prov-mismatch.json`: record with PROV `subject_id` mismatch.
- `promotion-record-mismatch.json`: record with top-level `release_ref` mismatch.
- `missing-prov-decision.json`: decision payload missing `prov` catalog ref.

All fixtures are synthetic and safe for public test usage.
