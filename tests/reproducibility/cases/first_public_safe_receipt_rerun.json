{
  "case_id": "first_public_safe_receipt_rerun",
  "description": "Compare two fixture-backed run receipts for deterministic identity.",
  "network": "off_by_default",
  "expected_outcome": "PASS",
  "must_match_fields": [
    "object_type",
    "spec_hash",
    "decision",
    "artifact_digests",
    "policy_refs"
  ],
  "allowed_drift_fields": [
    "generated_at"
  ]
}
