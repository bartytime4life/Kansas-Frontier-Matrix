# Reproducibility Reports

Machine-readable comparison outputs generated by `tests/reproducibility/scripts/compare_receipts.py`.

Use transient report files during local runs and keep only small review fixtures in git.
