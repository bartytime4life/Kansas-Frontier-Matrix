# Local data capture example

These synthetic bytes exercise the offline quarantine importer. They are not
Kansas map data, an admitted source, or a published product. The example
manifest in `configs/examples/local-data-manifest.json` binds their exact digest
and byte length. Tests create mutable and adversarial cases in temporary
directories; no real source files or private metadata belong here.
