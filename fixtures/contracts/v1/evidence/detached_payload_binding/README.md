# DetachedPayloadBinding fixtures

Synthetic, no-network JSON and detached bytes used to prove closed-schema validation, safe and canonical mirror metadata, deterministic IDs, raw-byte SHA-256 and size verification, HOLD on missing local bytes, and authority denial. The URLs are non-routable examples and are never fetched. The fixtures are not EvidenceBundles, signatures, source authority, release records, or public artifacts.
