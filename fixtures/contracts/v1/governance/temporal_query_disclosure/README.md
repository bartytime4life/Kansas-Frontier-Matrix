# TemporalQueryDisclosure fixtures

Synthetic no-network cases cover all five temporal query classes plus negative cases for missing class-specific fields, invalid clock bases, reversed intervals, insufficient snapshot history, explanation-code drift, unsorted collections, and arbitrary public prose.

The fixtures do not execute SQL, read a database, resolve evidence, infer release state, generate public claims, or authorize publication.
