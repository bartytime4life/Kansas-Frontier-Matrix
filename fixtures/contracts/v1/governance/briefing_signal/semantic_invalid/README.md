# Semantically invalid BriefingSignal fixtures

These candidates pass Draft 2020-12 shape validation but fail deterministic semantic validation. Each JSON file has an adjacent `.expected.txt` sidecar containing exactly `CODE<TAB>JSON_PATH`.

The set covers materiality score, priority, reason-code, mandatory-override context, routing disposition, and routing-reason-code drift. Values are synthetic and findings must not echo candidate prose.
