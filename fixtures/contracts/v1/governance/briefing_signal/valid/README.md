# Valid BriefingSignal fixtures

The valid set covers new, duplicate, corrective, source-discovery, dependency-hold, and unsafe-rejection routes. Every record is synthetic, deterministic, no-network, and explicitly denies repository mutation, source activation, proof, release, deployment, publication, and public use.

The fixtures prove routing diversity without executing any operation. The duplicate follow-up intentionally changes its headline and daily `signal_id` while preserving the primary signal's `event_cluster_id`.
