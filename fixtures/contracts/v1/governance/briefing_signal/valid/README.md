# Valid BriefingSignal fixtures

The valid set contains one new-cluster signal and one later duplicate revision. Both are synthetic, deterministic, no-network, and explicitly deny repository mutation, source activation, proof, release, deployment, publication, and public use.

The follow-up intentionally changes its headline and daily `signal_id` while preserving the first signal's `event_cluster_id`. It references the primary signal and proposes `NO_ACTION`, proving that repeated coverage need not create repeated work.
