# Schema-invalid SourceActivationDecision fixtures

Each candidate must fail Draft 2020-12 schema validation. `.expected_error.txt` supports the repository common-contract harness; `.expected_findings.txt` pins the dedicated validator outcome.
