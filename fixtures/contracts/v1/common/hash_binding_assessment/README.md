# Hash-binding assessment fixtures

These fixtures exercise the inactive `HashBindingAssessment` contract with no network access, production data, digest migration, signature, release, or publication effect.

- `valid_assessment.json` binds the six profiles already present in the repository readiness matrix and holds normalized-geometry and receipt-payload identities where no dedicated profile exists.
- `cases.json` mutates that baseline to prove deterministic rejection of self-hashing fields, recursive signature subjects, volatile timestamps, unordered collections, non-finite-number posture, ambiguous geometry controls, cross-profile comparisons, readiness drift, unauthorized binding, authority overreach, and stale self-hashes.

Fixture `PASS` proves conformance only. It does not prove content truth, source authority, evidence sufficiency, algorithm availability, profile activation, migration compatibility, policy approval, release, or public safety.
