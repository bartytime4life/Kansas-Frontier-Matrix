# Valid fixtures

Four synthetic candidates cover an observed condition, a future-effective regulatory record, a corrected revision, and an unresolved briefing candidate. Source-role meaning remains bound by `source_role_ref` to the declared SourceDescriptor instead of creating a second role vocabulary.
