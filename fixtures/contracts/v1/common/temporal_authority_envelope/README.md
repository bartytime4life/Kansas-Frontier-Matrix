# TemporalAuthorityEnvelope fixtures

Synthetic, no-network fixtures for the proposed shared metadata envelope.

- `valid/` contains four accepted profiles.
- `invalid/` contains seven schema-invalid candidates with generic schema sidecars and exact validator-code sidecars.
- `semantic_invalid/` contains seven schema-valid candidates rejected by cross-field semantics.

All records are synthetic. They do not represent source admission, evidence resolution, policy approval, review, release, or publication.
