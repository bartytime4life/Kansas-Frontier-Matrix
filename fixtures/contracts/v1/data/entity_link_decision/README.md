# Entity link-decision fixtures

**Status:** PROPOSED · synthetic · fixture-only · no canonical mutation.

The valid corpus covers `PROPOSE_MERGE`, `REVIEW`, `REJECT`, and `HOLD`. The invalid corpus verifies threshold, deterministic-check, evidence, conflict, canonical pair ordering, governance, and identity boundaries.

The fixtures use synthetic record and EvidenceRef identifiers. They contain no real people, parcels, facilities, locations, source payloads, credentials, or release authority.
