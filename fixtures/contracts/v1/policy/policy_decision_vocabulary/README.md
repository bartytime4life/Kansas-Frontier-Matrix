# PolicyDecision vocabulary fixtures

Synthetic, no-network JSON Schema fixtures for the inactive vocabulary profile.

The dedicated validator tests exercise sorted code sets, namespace collisions, obligation outcome polarity, and governance overclaim behavior in memory. These fixtures contain no real policy decision, sensitive value, release approval, or public-use authority.
