# PolicyDecision semantics v1 fixtures

Synthetic, no-network records for testing the existing `PolicyDecision` schema against the inactive reason/obligation vocabulary.

The dedicated tests derive negative cases in memory for unknown codes, outcome/family mismatch, missing negative reasons, negative obligations, missing answer obligations, and noncanonical code ordering. No fixture represents an authenticated policy evaluation, release approval, or public-use decision.
