# Evidence resolver fixtures

Synthetic inputs for `packages/evidence-resolver/`. The only admitted profile
is [`v1alpha1/`](v1alpha1/), an internal compatibility surface. These records
are not evidence, registry rows, proof objects, policy decisions, review
records, release records, or public responses.
