# Habitat patch — valid fixtures

`fixtures/domains/habitat/patch/valid/`

Status: draft / fixture lane, first payload added.

| File | Scenario | Expected outcome |
|---|---|---|
| `valid_1_habitat_patch.json` | Synthetic `HabitatPatch` example, same object family as `../../valid/valid_1_habitat_patch.json`. | Passes shape check; not released. |

See also: [`../../valid/README.md`](../../valid/README.md)
