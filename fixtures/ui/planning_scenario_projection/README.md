# Planning scenario projection fixtures

Synthetic, fixture-only display projections for the inactive Explorer Web planning-scenario review. The available fixture is a bounded projection of the validated water-planning manifest and exposes its generalized horizon, explicit inputs, uncertainty, assumptions, equity questions, participation references, evidence references, and limitations without adding recommendation or publication authority.

The held review preserves the manifest's `ABSTAIN` outcome while exposing bounded synthetic detail. Valid missing, denied, and error fixtures carry only finite outcome metadata and no scenario detail. Invalid fixtures prove that unexpected fields and undeclared evidence references fail closed without reflecting their canaries into UI state.
