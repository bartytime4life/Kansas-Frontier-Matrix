# ---------------------------------------
# Python
# ---------------------------------------
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
.buildozer/
__pypackages__/

# Tooling caches
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.cache/

# ---------------------------------------
# Git / CI / Editors
# ---------------------------------------
.git
.gitignore
.gitattributes
.github/
.devcontainer/
.vscode/
.idea/
.editorconfig
.pre-commit-config.yaml

# ---------------------------------------
# Envs / logs
# ---------------------------------------
.venv/
venv/
.env
.env.*
*.log

# ---------------------------------------
# Build outputs
# ---------------------------------------
build/
dist/
site/                 # MkDocs output
*.whl
*.egg
*.tar.gz

# CI/test leftovers
pytest-report.xml
coverage.xml

# ---------------------------------------
# Node / web
# (container should not bundle dev deps or local builds)
# ---------------------------------------
node_modules/
web/node_modules/
web/.cache/
web/.vite/
web/.next/
web/dist/
web/build/
web/tiles/            # tile pyramids (heavy)
web/assets/tiles/
web/.DS_Store

# ---------------------------------------
# DVC (keep .dvc files outside this dir)
# ---------------------------------------
.dvc/

# ---------------------------------------
# Data (mount at runtime)
# Keep catalogs if you validate them in-container
# ---------------------------------------
data/**

# allow small JSON catalogs / descriptors / DVC metafiles
!data/sources/**
!data/**/*.dvc

# ---------------------------------------
# STAC catalogs are small: keep them
# ---------------------------------------
!stac/**

# but still exclude any large rasters/archives under those trees
stac/*.tif
stac/*.tiff
stac/*.vrt
stac/*.ovr
stac/*.aux.xml
stac/*.zip
stac/*.7z
stac/*.tar
stac/*.tar.gz
stac/**/*.tif
stac/**/*.tiff
stac/**/*.vrt
stac/**/*.ovr
stac/**/*.aux.xml
stac/**/*.zip
stac/**/*.7z
stac/**/*.tar
stac/**/*.tar.gz

data/sources/*.tif
data/sources/*.tiff
data/sources/*.vrt
data/sources/*.ovr
data/sources/*.aux.xml
data/sources/*.zip
data/sources/*.7z
data/sources/*.tar
data/sources/*.tar.gz
data/sources/**/*.tif
data/sources/**/*.tiff
data/sources/**/*.vrt
data/sources/**/*.ovr
data/sources/**/*.aux.xml
data/sources/**/*.zip
data/sources/**/*.7z
data/sources/**/*.tar
data/sources/**/*.tar.gz

# legacy/derived outputs (always ignore)
data/cogs/
data/processed/
data/outputs/
data/raw/
assets/tiles/

# ---------------------------------------
# Rasters and big geodata anywhere
# ---------------------------------------
*.tif
*.tiff
*.vrt
*.ovr
*.aux.xml
*.tfw
*.idx
*.lock

# ---------------------------------------
# Archives
# ---------------------------------------
*.zip
*.7z
*.tar
*.tar.gz
*.tar.xz
*.gz

# ---------------------------------------
# Earth / KML big artifacts
# (generated; mount or publish separately)
# ---------------------------------------
earth/*.kmz
earth/*.kml
earth/**/*.kmz
earth/**/*.kml
earth/networklinks/

# ---------------------------------------
# Notebooks + checkpoints
# ---------------------------------------
*.ipynb
.ipynb_checkpoints/

# ---------------------------------------
# OS cruft
# ---------------------------------------
.DS_Store
Thumbs.db
