# Cosign bundle output directory

`make sign` writes these files here:

- `evidencebundle.bundle`
- `decision_log.bundle`

The starter archive intentionally does not include fake signature bundles. Generate real bundles with a real Sigstore identity or key before production publication.
