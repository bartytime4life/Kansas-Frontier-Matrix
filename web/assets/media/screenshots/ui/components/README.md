---
title: 🧩 UI Component Screenshots
path: web/assets/media/screenshots/ui/components
status: active
owner: Web/UI
last_updated: 2026-01-18
---

# 🧩 UI Component Screenshots

![Asset Type](https://img.shields.io/badge/assets-screenshots-informational)
![Scope](https://img.shields.io/badge/scope-ui%2Fcomponents-blue)
![Format](https://img.shields.io/badge/format-PNG%20%7C%20WEBP-success)
![Intent](https://img.shields.io/badge/intent-docs%20%2B%20PR%20evidence%20%2B%20regression-yellow)

> [!NOTE]
> This folder is the **canonical home** for *version-controlled* screenshots of **reusable UI components** (not full-page flows).  
> Use these for 📚 documentation, 🧪 visual regression baselines, and 🧾 PR/issue evidence.

---

## 🎯 What this folder is for

✅ **In scope**
- **Reusable components** (buttons, panels, modals, timeline controls, map widgets, etc.)
- **Stateful variants** (hover/focus/disabled/loading/error/empty)
- **Theme variants** (light/dark)
- **Viewport variants** (desktop/tablet/mobile)
- **Evidence-grade screenshots** that help reviewers confirm UI behavior and accessibility

🚫 **Out of scope**
- Whole-page walkthroughs / flows (these should live in a *page/flow* screenshots area, not in `components/`)
- One-off debugging captures (“look at this weird thing”)  
- Screenshots containing **PII**, secrets, or non-redacted sensitive content

---

## 📁 Folder layout contract

Use **one folder per component**. Keep files flat inside each component folder.

```text
📁 web/assets/media/screenshots/ui/components/
├── 📄 README.md
├── 📁 <component-name>/
│   ├── 🖼️ <component>--<variant>--<state>--<theme>--<viewport>.png
│   ├── 🖼️ <component>--<variant>--<state>--<theme>--<viewport>.webp
│   ├── 🧾 _meta.json                (optional but encouraged ✨)
│   └── 🧾 _notes.md                 (optional: why this screenshot exists)
└── 📁 _shared/                      (optional: tokens, overlays, shared fixtures)
```

---

## 🏷️ Naming convention

**Goal:** filenames should be readable, sortable, and diffable at a glance.

### ✅ Template

```text
<component>--<variant>--<state>--<theme>--<viewport>[--<locale>][--<data>].png
```

### 🔤 Rules
- Use **kebab-case** everywhere (`layer-panel`, not `LayerPanel`).
- Use `--` as the **token separator**.
- Prefer short, consistent tokens over prose.
- If a token doesn’t apply, omit it (don’t invent “none”).

### 📌 Required tokens (minimum)
- `<component>` (e.g., `timeline`, `layer-panel`, `story-panel`)
- `<state>` (e.g., `default`, `hover`, `focus`, `disabled`, `loading`, `error`, `empty`)
- `<theme>` (`light` or `dark`)
- `<viewport>` (see recommended list below)

### ✨ Examples
```text
button--primary--default--light--desktop-1440x900.png
button--primary--focus--light--desktop-1440x900.png
layer-panel--filters-open--default--dark--desktop-1440x900.png
timeline--scrubbing--active--dark--tablet-1024x768.png
focus-mode--answer--default--light--desktop-1440x900--en-US.png
```

---

## 📐 Capture standards

> [!IMPORTANT]
> These screenshots are **evidence artifacts**. Treat them like “UI test fixtures”: stable, intentional, and reproducible.

### 🖥️ Recommended viewports (pick from these)
- `desktop-1440x900`
- `tablet-1024x768`
- `mobile-390x844`

If you need a new viewport, add it deliberately and reuse it consistently across components.

### 🌗 Themes
- Capture **light** and **dark** when the component supports them.
- If the component has only one theme, capture that one and omit the other.

### 🧼 Data hygiene (privacy + safety)
- Use **sanitized demo data** only.
- Never include:
  - personal names, emails, addresses, phone numbers
  - API keys, tokens, connection strings
  - internal-only dataset URLs or private tile endpoints

### ♿ Accessibility evidence (highly recommended)
Capture a few “proof” states when relevant:
- `focus` (visible focus ring)
- `error` (clear message + iconography)
- `disabled` (contrast still acceptable)
- `empty` (helpful empty-state copy)

---

## 🧾 Optional metadata sidecar

For components that matter to reviews/regression, add a `_meta.json` next to the images.

### ✅ Suggested `_meta.json` schema (lightweight)

```json
{
  "component": "layer-panel",
  "screenshots": [
    {
      "file": "layer-panel--filters-open--default--dark--desktop-1440x900.png",
      "state": "default",
      "variant": "filters-open",
      "theme": "dark",
      "viewport": "desktop-1440x900",
      "source": {
        "route": "/",
        "storybook": "ui/layer-panel--filters-open"
      },
      "data_profile": "demo-sanitized"
    }
  ]
}
```

> [!TIP]
> Metadata is especially useful when screenshots are generated by automation (Playwright/Cypress/Storybook) and you want a durable breadcrumb trail.

---

## 🧷 Using screenshots in docs

### ✅ Markdown embed pattern

```md
![Layer Panel — filters open (dark, desktop)](./layer-panel/layer-panel--filters-open--default--dark--desktop-1440x900.png)
```

### ✍️ Caption convention (optional)
```md
*Figure: Layer Panel with filters expanded (dark theme, desktop).*
```

---

## 🔄 Update workflow

When UI behavior changes:
1. **Update** the corresponding screenshot(s) in this folder (don’t leave stale evidence).
2. In your PR description, include:
   - the **new screenshots** (inline or linked)
   - a short note about what changed (state/behavior/visual)
3. Keep changes scoped:
   - avoid “drive-by” re-captures of unrelated components unless you’re doing a coordinated UI refresh

> [!WARNING]
> Large binary churn makes reviews painful. Only touch what you mean to change.

---

## ✅ Screenshot Definition of Done

Use this checklist before committing:

- [ ] File name follows the convention (`component--variant--state--theme--viewport.png`)
- [ ] Image is cropped to the **component boundary** (no extra whitespace unless it’s part of the component)
- [ ] No PII / secrets / private endpoints visible
- [ ] Theme + viewport tokens are accurate
- [ ] If the component is interactive, a meaningful state (`hover` / `focus` / `active`) is captured
- [ ] PNG/WebP is optimized (reasonable file size)
- [ ] Screenshot is referenced in a doc, issue, or PR (or is a regression baseline)

---

## 🧭 Related docs & standards

- 🧱 Repo structure & documentation standards: `/docs/standards/`
- 📝 Markdown authoring conventions: `/docs/standards/KFM_MARKDOWN_WORK_PROTOCOL.md`
- 🧬 Provenance / traceability mindset: `/docs/standards/` (PROV / profiles)

---

## 🧠 Quick FAQ

<details>
  <summary><strong>Why keep screenshots in Git instead of only in PR comments?</strong> 📌</summary>

- PR screenshots are great for review, but **version-controlled** screenshots become:
  - documentation assets 📚
  - long-term UI evidence 🧾
  - baseline fixtures for regression 🧪

</details>

<details>
  <summary><strong>PNG or WebP?</strong> 🖼️</summary>

- Use **PNG** for crisp UI (text, icons, sharp edges).
- Use **WebP** if the screenshot is photo-heavy and PNG is huge.
- Don’t mix formats for the same component/state unless there’s a clear reason.

</details>
