# Kansas Frontier Matrix (KFM)

KFM is **Kansas-first, map-first, time-aware, evidence-first, trust-visible, governed, auditable, and reversible**.

## Lifecycle law
`SOURCE EDGE -> RAW -> WORK / QUARANTINE -> PROCESSED -> CATALOG / TRIPLET -> PUBLISHED`

Promotion is a governed state transition.

## Quickstart
- `python tools/validate_repo.py`
- `python tools/validate_fixtures.py`
- `python tools/check_directory_rules.py`
- `python tools/check_no_public_internal_paths.py`
- `python -m unittest discover -s tests`

## Proof slice
This baseline is a no-network, synthetic, hydrology-first proof lane: source descriptor -> evidence -> validation -> policy -> release dry run -> governed API/mock UI -> Focus outcome -> rollback continuity.
