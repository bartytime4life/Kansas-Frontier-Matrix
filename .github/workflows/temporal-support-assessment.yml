# Validate fixture-first temporal support assessments.
name: temporal-support-assessment

"on":
  pull_request:
    paths:
      - ".github/workflows/temporal-support-assessment.yml"
      - "contracts/evidence/temporal_support_assessment.md"
      - "schemas/contracts/v1/evidence/temporal_support_assessment.schema.json"
      - "tools/validators/evidence/validate_temporal_support_assessment.py"
      - "fixtures/contracts/v1/evidence/temporal_support_assessment/**"
      - "tests/evidence/test_temporal_support_assessment.py"
      - "data/receipts/generated/genrec-temporal-support-assessment-*.json"
  push:
    branches: [main]
    paths:
      - ".github/workflows/temporal-support-assessment.yml"
      - "contracts/evidence/temporal_support_assessment.md"
      - "schemas/contracts/v1/evidence/temporal_support_assessment.schema.json"
      - "tools/validators/evidence/validate_temporal_support_assessment.py"
      - "fixtures/contracts/v1/evidence/temporal_support_assessment/**"
      - "tests/evidence/test_temporal_support_assessment.py"
      - "data/receipts/generated/genrec-temporal-support-assessment-*.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: temporal-support-assessment-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  KFM_NO_NETWORK: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  PYTHONUNBUFFERED: "1"
  TZ: UTC

jobs:
  validate:
    name: validate-temporal-support-assessment
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install declared test dependencies
        run: python -m pip install -e ".[test]"
      - name: Run fixture matrix
        run: python tools/validators/evidence/validate_temporal_support_assessment.py --fixtures
      - name: Run focused tests
        run: python -m pytest tests/evidence/test_temporal_support_assessment.py -q --strict-config --strict-markers
      - name: Validate generated authoring receipt
        run: python tools/validators/validate_generated_receipt.py data/receipts/generated/genrec-temporal-support-assessment-20260807.json --repo-root .
      - name: Record trust boundary
        if: always()
        run: |
          {
            echo "### Temporal support assessment"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- Scope: synthetic fixtures and local validation only."
            echo "- Boundary: no lifecycle mutation, correction approval, cache change, release, deployment, or publication."
          } >> "$GITHUB_STEP_SUMMARY"
