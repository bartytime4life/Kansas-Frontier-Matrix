# Validate the inactive fixture-only Soil support-type anti-collapse profile.
name: soil-support-type-profile

"on":
  pull_request:
    paths:
      - ".github/workflows/soil-support-type-profile.yml"
      - "contracts/domains/soil/support_type_profile.md"
      - "schemas/contracts/v1/domains/soil/support_type_*.schema.json"
      - "pipeline_specs/soil/support_type_profile.v1.json"
      - "tools/validators/domains/soil/support_type/**"
      - "fixtures/domains/soil/support_type/**"
      - "tests/validators/domains/soil/support_type/**"
      - "data/receipts/generated/genrec-soil-support-type-profile-*.json"
  push:
    branches: [main]
    paths:
      - ".github/workflows/soil-support-type-profile.yml"
      - "contracts/domains/soil/support_type_profile.md"
      - "schemas/contracts/v1/domains/soil/support_type_*.schema.json"
      - "pipeline_specs/soil/support_type_profile.v1.json"
      - "tools/validators/domains/soil/support_type/**"
      - "fixtures/domains/soil/support_type/**"
      - "tests/validators/domains/soil/support_type/**"
      - "data/receipts/generated/genrec-soil-support-type-profile-*.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: soil-support-type-profile-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  KFM_NO_NETWORK: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  PYTHONUNBUFFERED: "1"
  TZ: UTC

jobs:
  validate:
    name: validate-soil-support-type-profile
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install declared test dependencies
        run: python -m pip install -e ".[test]"

      - name: Run deterministic no-network profile tests
        run: |
          set -euo pipefail
          python -m unittest discover \
            --start-directory tests/validators/domains/soil/support_type \
            --pattern 'test_support_type_profile.py' \
            --verbose
          python tools/validators/domains/soil/support_type/validate_support_type_profile.py \
            --fixtures

      - name: Verify generated authoring receipt integrity
        run: |
          set -euo pipefail
          python tools/validators/validate_generated_receipt.py \
            data/receipts/generated/genrec-soil-support-type-profile-20260805.json \
            --repo-root .

      - name: Record trust boundary
        if: always()
        run: |
          {
            echo "### Soil support-type profile"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- Scope: inactive synthetic anti-collapse profile only."
            echo "- Boundary: a green result creates no source, evidence, policy, promotion, release, publication, or public-use authority."
          } >> "$GITHUB_STEP_SUMMARY"
