# Validate the fixture-only PopulationObservation candidate.
name: population-observation

"on":
  pull_request:
    paths:
      - ".github/workflows/population-observation.yml"
      - "contracts/evidence/population_observation.md"
      - "schemas/contracts/v1/evidence/population_observation.schema.json"
      - "fixtures/contracts/v1/evidence/population_observation/**"
      - "tools/validators/evidence/validate_population_observation.py"
      - "tests/validators/evidence/test_validate_population_observation.py"
      - "docs/intake/exploratory/implementation-reference-population-observation-source-map.md"
      - "contracts/common/geography_version.md"
      - "schemas/contracts/v1/common/geography_version.schema.json"
      - "packages/hashing/**"
      - "pyproject.toml"
      - "data/receipts/generated/genrec-implementation-reference-population-observation-20260810.json"
  push:
    branches: [main]
    paths:
      - ".github/workflows/population-observation.yml"
      - "contracts/evidence/population_observation.md"
      - "schemas/contracts/v1/evidence/population_observation.schema.json"
      - "fixtures/contracts/v1/evidence/population_observation/**"
      - "tools/validators/evidence/validate_population_observation.py"
      - "tests/validators/evidence/test_validate_population_observation.py"
      - "docs/intake/exploratory/implementation-reference-population-observation-source-map.md"
      - "contracts/common/geography_version.md"
      - "schemas/contracts/v1/common/geography_version.schema.json"
      - "packages/hashing/**"
      - "pyproject.toml"
      - "data/receipts/generated/genrec-implementation-reference-population-observation-20260810.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: population-observation-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  KFM_NO_NETWORK: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  PYTHONUNBUFFERED: "1"
  TZ: UTC

jobs:
  validate:
    name: validate-population-observation
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install declared validation dependencies
        run: python -m pip install -e ".[test]"
      - name: Run focused and adjacent deterministic tests
        run: |
          python -m unittest -v tests.validators.evidence.test_validate_population_observation
          python -m unittest -v tests.validators.test_validate_geography_version
          python tools/validators/evidence/validate_population_observation.py --fixtures
      - name: Validate generated authoring receipt integrity
        run: |
          python tools/validators/validate_generated_receipt.py \
            data/receipts/generated/genrec-implementation-reference-population-observation-20260810.json \
            --repo-root .
      - name: Record trust boundary
        if: always()
        run: |
          {
            echo "### PopulationObservation"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- Scope: fixture-only aggregate Kansas county-year population state, source timing, missingness, uncertainty, correction lineage, disclosure, and deterministic identity."
            echo "- Boundary: no live source access, geography or evidence resolution, person/household inference, cross-version join, frontier classification, policy evaluation, review approval, promotion, release, public use, publication, or deployment authority."
          } >> "$GITHUB_STEP_SUMMARY"
