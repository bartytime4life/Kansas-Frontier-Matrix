# KFM CI workflow stub: rollback-drill
# Status: PROPOSED greenfield scaffold.
name: rollback-drill

on:
  pull_request:
  push:
    branches: [main]

jobs:
  simulate-rollback:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO simulate-rollback'
  verify-published-aliases:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO verify-published-aliases'
