# KFM CI workflow stub: domain-atmosphere
# Status: PROPOSED greenfield scaffold.
name: domain-atmosphere

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate-atmosphere:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO validate-atmosphere'
  build-proof-atmosphere:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO build-proof-atmosphere'
  publish-dry-run-atmosphere:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO publish-dry-run-atmosphere'
