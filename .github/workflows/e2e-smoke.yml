# KFM CI workflow stub: e2e-smoke
# Status: PROPOSED greenfield scaffold.
name: e2e-smoke

on:
  pull_request:
  push:
    branches: [main]

jobs:
  spin-up-mock-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO spin-up-mock-runtime'
  run-e2e-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO run-e2e-smoke'
