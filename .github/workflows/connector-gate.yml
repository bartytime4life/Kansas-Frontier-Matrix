# KFM CI workflow stub: connector-gate
# Status: PROPOSED greenfield scaffold.
name: connector-gate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  connector-output-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO connector-output-gate'
  ingest-receipt-presence:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO ingest-receipt-presence'
