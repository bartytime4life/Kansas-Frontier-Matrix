# Validate the proposed Validator assurance report profile.
#
# This workflow is read-only, deterministic, synthetic, and no-network after
# installing repository-declared test dependencies. A green result proves only
# the bounded schema/fixture/validator invariants named below. It grants no
# source, evidence, policy, review, promotion, release, deployment, publication,
# legal, title, boundary, identity, or public-use authority.
name: validator-assurance-report

"on":
  pull_request:
    paths:
      - ".github/workflows/validator-assurance-report.yml"
      - "contracts/validation/README.md"
      - "contracts/validation/validator_assurance_report.md"
      - "schemas/contracts/v1/validation/README.md"
      - "schemas/contracts/v1/validation/validator_assurance_report.schema.json"
      - "fixtures/contracts/v1/validation/validator_assurance_report/**"
      - "tools/validators/validate_validator_assurance_report.py"
      - "tests/validators/test_validate_validator_assurance_report.py"
      - "data/receipts/generated/genrec-validator-assurance-report-*.json"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/validator-assurance-report.yml"
      - "contracts/validation/README.md"
      - "contracts/validation/validator_assurance_report.md"
      - "schemas/contracts/v1/validation/README.md"
      - "schemas/contracts/v1/validation/validator_assurance_report.schema.json"
      - "fixtures/contracts/v1/validation/validator_assurance_report/**"
      - "tools/validators/validate_validator_assurance_report.py"
      - "tests/validators/test_validate_validator_assurance_report.py"
      - "data/receipts/generated/genrec-validator-assurance-report-*.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validator-assurance-report-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  KFM_NO_NETWORK: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  PYTHONUNBUFFERED: "1"
  TZ: UTC

jobs:
  validate:
    name: validate-validator-assurance-report
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install declared schema and test dependencies
        shell: bash
        run: python -m pip install -e ".[test]"

      - name: Run focused no-network tests
        shell: bash
        run: |
          set -euo pipefail
          python -m unittest discover \
            --start-directory tests/validators \
            --pattern 'test_validate_validator_assurance_report.py' \
            --verbose

      - name: Validate exact fixture polarity and reviewed finding codes
        shell: bash
        run: |
          set -euo pipefail
          python tools/validators/validate_validator_assurance_report.py --fixtures

      - name: Validate schema and fixture JSON syntax
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path
          from jsonschema import Draft202012Validator

          schema_path = Path("schemas/contracts/v1/validation/validator_assurance_report.schema.json")
          Draft202012Validator.check_schema(
              json.loads(schema_path.read_text(encoding="utf-8"))
          )
          files = sorted(Path("fixtures/contracts/v1/validation/validator_assurance_report").rglob("*.json"))
          assert files, "fixture inventory is empty"
          for path in files:
              json.loads(path.read_text(encoding="utf-8"))
          print(f"CONFIRMED: parsed schema and {len(files)} fixture JSON files.")
          PY

      - name: Verify generated authoring receipt integrity
        shell: bash
        run: |
          set -euo pipefail
          python tools/validators/validate_generated_receipt.py \
            data/receipts/generated/genrec-validator-assurance-report-20260805.json \
            --repo-root .

      - name: Record validation boundary
        if: always()
        shell: bash
        run: |
          {
            echo "### Validator assurance report"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- Scope: deterministic campaign identity, operator inventory, mutant arithmetic, survivor semantic-risk classification, finite assurance outcomes, no-universal-threshold posture, and authority separation."
            echo "- Network posture: no live sources, external services, evidence stores, policy engines, release systems, or public endpoints."
            echo "- Boundary: a green result does not grant source authority, evidence closure, policy approval, human review, promotion, release, deployment, publication, or public use."
          } >> "$GITHUB_STEP_SUMMARY"
