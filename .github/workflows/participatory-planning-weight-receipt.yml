name: participatory-planning-weight-receipt

"on":
  pull_request:
    paths:
      - ".github/workflows/participatory-planning-weight-receipt.yml"
      - "contracts/evidence/participatory_planning_weight_receipt.md"
      - "schemas/contracts/v1/evidence/participatory_planning_weight_receipt.schema.json"
      - "fixtures/contracts/v1/evidence/participatory_planning_weight_receipt/**"
      - "tools/validators/evidence/validate_participatory_planning_weight_receipt.py"
      - "tests/validators/evidence/test_validate_participatory_planning_weight_receipt.py"
      - "docs/intake/exploratory/pass-18-participatory-planning-weight-receipt-source-map.md"
      - "data/receipts/generated/genrec-pass18-participatory-planning-weight-receipt-*.json"
      - "pyproject.toml"
  push:
    branches: [main]
    paths:
      - ".github/workflows/participatory-planning-weight-receipt.yml"
      - "contracts/evidence/participatory_planning_weight_receipt.md"
      - "schemas/contracts/v1/evidence/participatory_planning_weight_receipt.schema.json"
      - "fixtures/contracts/v1/evidence/participatory_planning_weight_receipt/**"
      - "tools/validators/evidence/validate_participatory_planning_weight_receipt.py"
      - "tests/validators/evidence/test_validate_participatory_planning_weight_receipt.py"
      - "docs/intake/exploratory/pass-18-participatory-planning-weight-receipt-source-map.md"
      - "data/receipts/generated/genrec-pass18-participatory-planning-weight-receipt-*.json"
      - "pyproject.toml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: participatory-planning-weight-receipt-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  KFM_NO_NETWORK: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  TZ: UTC

jobs:
  validate:
    name: validate-participatory-planning-weight-receipt
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install declared validation dependencies
        run: python tools/ci/install_python_ci.py project-test
      - name: Run focused deterministic validation
        run: |
          set -euo pipefail
          python -m py_compile tools/validators/evidence/validate_participatory_planning_weight_receipt.py tests/validators/evidence/test_validate_participatory_planning_weight_receipt.py
          python -m unittest tests.validators.evidence.test_validate_participatory_planning_weight_receipt --verbose
          python tools/validators/evidence/validate_participatory_planning_weight_receipt.py --fixtures
      - name: Validate generated authoring receipt
        run: |
          set -euo pipefail
          python tools/validators/validate_generated_receipt.py data/receipts/generated/genrec-pass18-participatory-planning-weight-receipt-20260811.json --repo-root .
      - name: Record trust boundary
        if: always()
        run: |
          {
            echo "### Participatory planning weight receipt candidate"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- Scope: fixture-only stakeholder-group identity, criteria and weight normalization, facilitation, consent, dissent, sensitivity, canonical ordering, and declaration coherence."
            echo "- Boundary: no participant collection, consensus claim, evidence resolution, preference aggregation, suitability score, planning or policy decision, review, promotion, release, deployment, publication, or public-use authority."
          } >> "$GITHUB_STEP_SUMMARY"
