# Focused no-network validation for the proposed RollbackCard candidate profile.
name: rollback-card

"on":
  pull_request:
    paths:
      - "**/*.md"
      - "contracts/release/rollback_card.md"
      - "schemas/contracts/v1/release/rollback_card.schema.json"
      - "fixtures/release/rollback_card/**"
      - "tools/validators/release/validate_rollback_card.py"
      - "tests/validators/test_validate_rollback_card.py"
      - "data/receipts/generated/genrec-rollback-card-contract-*.json"
      - ".github/workflows/rollback-card.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: rollback-card-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  PYTHONUNBUFFERED: "1"
  TZ: UTC

jobs:
  validate:
    name: validate-rollback-card
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install declared validator dependencies
        run: python tools/ci/install_python_ci.py project-runtime

      - name: Run focused tests and fixture polarity
        run: |
          set -euo pipefail
          python -m unittest discover \
            --start-directory tests/validators \
            --pattern 'test_validate_rollback_card.py' \
            --verbose
          python tools/validators/release/validate_rollback_card.py --fixtures

      - name: Verify RollbackCard contract current-binding receipt
        run: |
          set -euo pipefail
          python tools/validators/validate_generated_receipt.py \
            data/receipts/generated/genrec-rollback-card-contract-current-binding-20260815.json \
            --repo-root .

      - name: Record trust boundary
        if: always()
        run: |
          {
            echo "### RollbackCard candidate validation"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- A green result proves candidate shape, exact fixture polarity, local consistency, contract-to-profile documentation binding, and AI-authored artifact hash integrity only."
            echo "- No release, rollback executor, cache invalidator, public endpoint, review authority, or policy authority was invoked."
            echo "- It does not execute rollback, authorize release mutation, erase history, or publish."
          } >> "$GITHUB_STEP_SUMMARY"
