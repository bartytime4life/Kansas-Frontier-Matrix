# TEMPORARY inert staging helper. Replaced and removed before draft delivery.
name: _agent-temp-topology-evidence

"on":
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inert:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary helper is inert."
