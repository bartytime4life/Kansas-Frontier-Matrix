# KFM CI workflow stub: link-check
# Status: PROPOSED greenfield scaffold.
name: link-check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  docs-link-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO docs-link-check'
