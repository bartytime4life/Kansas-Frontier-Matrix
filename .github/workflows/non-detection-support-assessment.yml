# Validate fixture-first sampling-effort and non-detection support assessments.
name: non-detection-support-assessment

"on":
  pull_request:
    paths:
      - ".github/workflows/non-detection-support-assessment.yml"
      - "contracts/evidence/non_detection_support_assessment.md"
      - "schemas/contracts/v1/evidence/non_detection_support_assessment.schema.json"
      - "tools/validators/evidence/validate_non_detection_support_assessment.py"
      - "fixtures/contracts/v1/evidence/non_detection_support_assessment/**"
      - "tests/evidence/test_non_detection_support_assessment.py"
      - "docs/intake/exploratory/full-atlas-nondetection-support-source-map.md"
      - "data/receipts/generated/genrec-full-atlas-nondetection-support-*.json"
  push:
    branches: [main]
    paths:
      - ".github/workflows/non-detection-support-assessment.yml"
      - "contracts/evidence/non_detection_support_assessment.md"
      - "schemas/contracts/v1/evidence/non_detection_support_assessment.schema.json"
      - "tools/validators/evidence/validate_non_detection_support_assessment.py"
      - "fixtures/contracts/v1/evidence/non_detection_support_assessment/**"
      - "tests/evidence/test_non_detection_support_assessment.py"
      - "docs/intake/exploratory/full-atlas-nondetection-support-source-map.md"
      - "data/receipts/generated/genrec-full-atlas-nondetection-support-*.json"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: non-detection-support-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  KFM_NO_NETWORK: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  PYTHONUNBUFFERED: "1"
  TZ: UTC

jobs:
  validate:
    name: validate-non-detection-support
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install declared test dependencies
        run: python -m pip install -e ".[test]"
      - name: Run fixture matrix
        run: python tools/validators/evidence/validate_non_detection_support_assessment.py --fixtures
      - name: Run focused tests
        run: python -m pytest tests/evidence/test_non_detection_support_assessment.py -q --strict-config --strict-markers
      - name: Validate generated authoring receipt
        run: python tools/validators/validate_generated_receipt.py data/receipts/generated/genrec-full-atlas-nondetection-support-20260809.json --repo-root .
      - name: Record trust boundary
        if: always()
        run: |
          {
            echo "### Non-detection support assessment"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- Scope: synthetic sampling fixtures and local validation only."
            echo "- Boundary: no absence claim, source activation, lifecycle write, release, deployment, or publication."
          } >> "$GITHUB_STEP_SUMMARY"
