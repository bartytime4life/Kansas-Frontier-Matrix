# Validate the fixture-only Soil-Agriculture public-safe context candidate profile.
name: soil-agriculture-public-safe-context

"on":
  pull_request:
    paths:
      - ".github/workflows/soil-agriculture-public-safe-context.yml"
      - "contracts/cross_domain/soil_agriculture/**"
      - "fixtures/contracts/v1/joins/soil_agriculture_public_safe_context/**"
      - "tests/cross_domain/soil_agriculture/**"
      - "tools/validators/cross_domain/soil_agriculture/**"
      - "contracts/joins/cross_lane_join_assessment.md"
      - "schemas/contracts/v1/joins/cross_lane_join_assessment.schema.json"
      - "tools/joins/join_candidates.py"
      - "tests/joins/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: soil-agriculture-public-safe-context-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  KFM_NO_NETWORK: "1"
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PIP_NO_INPUT: "1"
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONHASHSEED: "0"
  TZ: UTC

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Check out tested revision without persisted credentials
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up repository Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: pyproject.toml
      - name: Install declared test dependencies
        run: python tools/ci/install_python_ci.py project-test
      - name: Run generic cross-lane fixture matrix
        run: python tools/joins/join_candidates.py --fixtures
      - name: Run Soil-Agriculture pair fixture matrix
        run: python tools/validators/cross_domain/soil_agriculture/validate_public_safe_context.py --fixtures
      - name: Run focused regression tests
        run: >-
          python -m pytest
          tests/joins/test_join_candidates.py
          tests/cross_domain/soil_agriculture/test_public_safe_context.py
          -q --strict-config --strict-markers
      - name: Record trust boundary
        if: always()
        run: |
          {
            echo "### Soil-Agriculture public-safe context candidate profile"
            echo
            echo "- Result: ${{ job.status }}."
            echo "- Scope: synthetic references and metadata only; no coordinate/geometry bytes, live source, network, or lifecycle write."
            echo "- Boundary: private farm/operator/parcel precision fails closed; ALLOW remains CANDIDATE_RELATION and grants no publication authority."
          } >> "$GITHUB_STEP_SUMMARY"
