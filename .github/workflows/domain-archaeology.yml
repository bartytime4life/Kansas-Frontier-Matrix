# KFM CI workflow stub: domain-archaeology
# Status: PROPOSED greenfield scaffold.
name: domain-archaeology

on:
  pull_request:
  push:
    branches: [main]

jobs:
  validate-archaeology:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO validate-archaeology'
  build-proof-archaeology:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO build-proof-archaeology'
  publish-dry-run-archaeology:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: 'echo TODO publish-dry-run-archaeology'
