# `MapContextEnvelope` object folder

The canonical semantic contract is the flat sibling [`../map_context_envelope.md`](../map_context_envelope.md). This folder remains a compatibility and navigation guard only; it does not define a second object meaning.

Machine shape lives at `schemas/contracts/v1/ui/map_context_envelope.schema.json`; fixture and validator paths are recorded in that schema. `MapContextEnvelope` is immutable released request context, not evidence, policy, review, release, an answer, or public-use authority.
