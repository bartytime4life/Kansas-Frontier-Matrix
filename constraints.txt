# ===========================================================
# Kansas Geo Timeline — pinned constraints for reproducible CI
# Apply with:
#   pip install -r requirements.txt --constraint constraints.txt
#
# Notes:
# - Raster/geo pins match manylinux/macos wheels (no system GDAL required).
# - NumPy stays on 1.26.x for rasterio 1.3.x ABI compatibility (<2.0).
# - Pins stac-validator + jsonschema stack so `stac-validate` is reproducible.
# - Locks common transitives to minimize resolver churn.
# Tested on Python 3.10 / 3.11 / 3.12.
# ===========================================================

############################
# Core numerical / GIS stack
############################
numpy==1.26.4
rasterio==1.3.10
rio-cogeo==5.3.3
shapely==2.0.4
pyproj==3.6.1
Pillow==10.4.0

# Rasterio/geo transitives
affine==2.4.0
attrs==24.2.0
cligj==0.7.2
snuggs==1.4.7

############################
# STAC / schema / validator
############################
pystac==1.10.1
pystac-client==0.7.6
stac-validator==3.3.1

jsonschema==4.23.0
jsonschema-specifications==2023.12.1
referencing==0.35.1
rpds-py==0.20.0

# time utils used across libs
python-dateutil==2.9.0.post0
six==1.16.0

############################
# CLI / utility
############################
click==8.1.7
rich==13.7.1
requests==2.32.3
tqdm==4.66.5
packaging==24.1

# Requests transitives
certifi==2024.8.30
charset-normalizer==3.3.2
idna==3.8
urllib3==2.2.3

# Rich transitives
Pygments==2.18.0
markdown-it-py==3.0.0
mdurl==0.1.2

# Windows-only console colors (tqdm/rich)
colorama==0.4.6; platform_system == "Windows"

# Typing backport for Python < 3.11
typing-extensions==4.12.2; python_version < "3.11"

############################
# Optional stacks (left unpinned)
############################
# geopandas, pyogrio, rio-tiler, titiler-core, httpx, jinja2, simplekml, etc.
