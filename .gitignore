# ================================
# Kansas-Frontier-Matrix — .gitignore
# ================================

# --- OS / Editors ---
.DS_Store
Thumbs.db
Icon?
ehthumbs.db
*.swp
*.swo
*.swn
*.tmp
*.bak
*.orig
.idea/
.vscode/
*.code-workspace
*.sublime-*
*.iml

# --- Python ---
__pycache__/
*.py[cod]
*.pyo
*.pyd
.ipynb_checkpoints/
.pytype/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.pyright/
.pyre/
.tox/
.nox/
.coverage*
htmlcov/
.cache/
.venv/
venv/
env/
pip-wheel-metadata/
.eggs/
*.egg-info/
.python-version
.envrc

# --- Node / Web ---
node_modules/
web/dist/
web/build/
web/.cache/
.pnpm-store/
.npm/
*.log

# Tiles & heavy web outputs (keep configs & small JSON)
web/tiles/
web/assets/tiles/
web/**/*.pmtiles
web/**/*.mbtiles

# If app.config.json is generated only in CI, you may ignore it;
# by default we KEEP it tracked for GitHub Pages:
# web/app.config.json

# --- Docs / MkDocs / Site artifacts ---
_site/
_site_docs/
site/
docs/_build/

# --- Data / GIS (scoped ignores; metadata like JSON/GeoJSON stay tracked) ---
# Avoid global *.tif/*.gpkg/etc ignores so Git-LFS can track when needed.
# Instead, ignore heavy binaries under known data/output dirs.

# Raw/processed/cogs/derivatives
data/raw/
data/outputs/
data/cogs/
data/derivatives/

# Block heavy binaries only within data/processed (allow .json/.geojson/.md)
data/processed/**/*.aux.xml
data/processed/**/*.ovr
data/processed/**/*.tif
data/processed/**/*.tiff
data/processed/**/*.vrt
data/processed/**/*.mbtiles
data/processed/**/*.pmtiles
data/processed/**/*.gpkg
data/processed/**/*.sqlite
data/processed/**/*.geoparquet
data/processed/**/*.parquet
data/processed/**/*.las
data/processed/**/*.laz

# Earth exports & caches (keep KML/KMZ examples elsewhere if desired)
earth/cache/
earth/exports/

# Archives & large bundles
*.zip
*.7z
*.tar
*.tar.gz
*.tgz
*.bz2
*.xz
*.zst

# --- DVC (keep config and .dvc files, ignore cache/tmp) ---
.dvc/cache/
.dvc/tmp/
*.dvc.lock

# --- Docker / Compose / Devcontainers ---
*.pid
docker/*.pid
docker/**/tmp/
.devcontainer/*/history.log

# --- Secrets & env (store securely; never commit) ---
.env
.env.*
*.env
*.key
*.pem
*.p12
*.crt
*.der
*.asc
*.gpg

# --- CI / Tooling caches ---
.pre-commit-*
coverage.xml
pytestdebug.log

# --- Binary docs (source lives elsewhere; commit selectively) ---
*.pdf
