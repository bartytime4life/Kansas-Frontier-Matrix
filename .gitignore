# ================================
# Kansas-Frontier-Matrix / MCP Repo
# ================================

# --- OS / Editor junk ---
.DS_Store
._*
Thumbs.db
Desktop.ini
*.swp
*.swo
*.bak
*.tmp
~$*
*.orig
.vscode/
.idea/

# --- Python artifacts / envs / logs ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.eggs/
.venv/
venv/
env/
pip-wheel-metadata/
.python-version
*.log
logs/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.tox/
.cache/
__pypackages__/

# Coverage
.coverage
.coverage.*
*.coverage
*.cover
*.cov
*.lcov
*.prof
*.out
coverage/
htmlcov/

# --- Node/JS frontend (web/) & generic build dirs ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
dist/
build/
web/.cache/
web/.vite/
web/.next/
web/dist/
web/build/
web/tiles/
web/assets/tiles/

# --- Docker / Compose ---
docker/*.log
docker/*.tmp
docker-compose.override.yml

# --- Catalogs / sources (keep small text descriptors) ---
# STAC lives at top-level `stac/` and SHOULD be tracked (no ignore for stac/**)
# Keep source descriptors under VCS, but ignore other random files in that dir
data/sources/*
!data/sources/**/*.json
!data/sources/**/*.jsonc
!data/sources/**/*.yaml
!data/sources/**/*.yml
!data/sources/**/*.toml

# --- Generated / transient data (ignore) ---
data/raw/
data/interim/
data/cache/
data/tmp/
data/derivatives/
data/outputs/
data/cogs/
data/processed/
data/tiles/
assets/tiles/

# GDAL aux / overviews / sidecars
*.aux.xml
*.ovr
*.tfw
*.wld

# Databases / locks / journal files
*.sqlite
*.db
*.gpkg
*.gpkg-journal
*.gpkg-wal
*.gpkg-shm
*.lock

# --- Rasters / vectors / containers (usually large/generated) ---
*.tif
*.tiff
*.vrt
*.img
*.bil
*.jp2
*.nc
*.h5
*.mbtiles
*.pmtiles
*.parquet
*.geoparquet
*.las
*.laz
*.kmz
*.kml
*.qgz
*.qgs

# Archives / bundles
*.zip
*.7z
*.tar
*.tar.gz
*.tgz
*.bz2
*.xz
*.zst

# Checksums & temp signatures
*.sha256

# --- Cloud Optimized GeoTIFF derivatives (often regenerated) ---
data/processed/**/*.tif
data/processed/**/*.tiff

# --- Jupyter / notebooks ---
.ipynb_checkpoints/
.notebooks_cache/
outputs/
*.nbconvert.ipynb

# --- Docs / static site builds ---
site/
docs/_build/
docs/_autosummary/
.mkdtemp/
*.doctree
*.pickle
_site/
.lychee_cache/

# --- Secrets / local overrides ---
.env
.env.local
.env.*.local
.envrc
config/local.yaml
config/secrets.yaml

# --- GitHub / automation (ignore caches only) ---
.github/workflows/*.log
.github/actions/cache/

# --- Project specific ---
web/*.local.js
web/*.config.local.json
mcp/experiments/*/outputs/
mcp/experiments/*/logs/
mcp/experiments/*/figures/

# --- DVC (track metadata, ignore cache/tmp) ---
# DO NOT ignore *.dvc files; they must be tracked
.dvc/cache/
.dvc/tmp/

# --- CI artifacts / reports ---
build/stac_report.json
pytest-report.xml
CHECKSUMS.txt

# --- Optional: allow curated tiny sample datasets under version control ----
# Place any small demo data in data/samples/** (overrides global ignores below).
!data/samples/**
!data/samples/**/*.json
!data/samples/**/*.geojson
!data/samples/**/*.csv
!data/samples/**/*.tsv
!data/samples/**/*.kml
!data/samples/**/*.kmz
!data/samples/**/*.tif
!data/samples/**/*.tiff
!data/samples/**/*.gpkg
!data/samples/**/*.fgb
