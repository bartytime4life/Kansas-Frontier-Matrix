# ================================
# Kansas-Frontier-Matrix / MCP Repo
# ================================

# --- OS/Editor junk ---
.DS_Store
Thumbs.db
*.swp
*.swo
*.bak
*.tmp
~$*
.vscode/
.idea/
*.orig

# --- Python artifacts / envs / logs ---
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.eggs/
.venv/
venv/
env/
pip-wheel-metadata/
.python-version
*.log

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.tox/
.cache/
__pypackages__/

# Coverage
.coverage
.coverage.*
*.coverage
*.cover
*.cov
*.lcov
*.prof
*.out
coverage/
htmlcov/

# --- Node/JS front-end (web/) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
web/.cache/
web/dist/
web/build/
dist/
build/

# --- Docker / Compose ---
docker/*.log
docker/*.tmp

# --- Catalogs / sources (keep small text descriptors) ---
# STAC lives at top-level `stac/` and SHOULD be tracked
# (no ignore rule for stac/** so it stays in Git)

# Keep source descriptors under VCS but not random files in that dir
data/sources/*
!data/sources/**/*.json
!data/sources/**/*.jsonc
!data/sources/**/*.yaml
!data/sources/**/*.yml
!data/sources/**/*.toml

# --- Generated / transient data (ignore) ---
data/raw/
data/interim/
data/cache/
data/tmp/
data/derivatives/
data/outputs/
data/cogs/
data/processed/
data/tiles/
assets/tiles/

# GDAL aux / overviews / sidecars
*.aux.xml
*.ovr
*.tfw

# Databases / locks / journal files
*.sqlite
*.db
*.gpkg
*.gpkg-journal
*.gpkg-wal
*.gpkg-shm
*.lock

# Rasters / vectors / containers (usually large/generated)
*.tif
*.tiff
*.vrt
*.img
*.bil
*.jp2
*.nc      # NetCDF
*.h5      # HDF5
*.mbtiles
*.parquet
*.geoparquet
*.las
*.laz
*.kmz
*.kml
*.qgz
*.qgs

# Archives / bundles
*.zip
*.7z
*.tar
*.tar.gz
*.tgz
*.bz2
*.xz
*.zst

# Checksums & temp signatures
*.sha256

# --- Cloud Optimized GeoTIFF derivatives (often regenerated) ---
data/processed/**/*.tif
data/processed/**/*.tiff

# --- Jupyter / notebooks ---
.ipynb_checkpoints/
.notebooks_cache/
outputs/
*.nbconvert.ipynb

# --- Docs / static site builds ---
site/
docs/_build/
docs/_autosummary/
.mkdtemp/
*.doctree
*.pickle
_site/

# --- Secrets / local overrides ---
.env
.env.local
.env.*.local
.envrc
config/local.yaml
config/secrets.yaml

# --- GitHub / automation (ignore caches only) ---
.github/workflows/*.log
.github/actions/cache/

# --- Project specific ---
web/*.local.js
web/*.config.local.json
mcp/experiments/*/outputs/
mcp/experiments/*/logs/

# --- DVC (track metadata, ignore cache/tmp) ---
# DO NOT ignore *.dvc files; they must be tracked
.dvc/cache/
.dvc/tmp/
