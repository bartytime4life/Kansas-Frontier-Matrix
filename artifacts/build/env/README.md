# artifacts/build/env

Build-environment fingerprints for outputs staged in `artifacts/build/pdf/` and `artifacts/build/dist/` belong here.

`tool-versions.yaml` records the proposed pinned toolchain. `build-env.json` records the per-build environment snapshot shape. These files support reproducibility, but they are not receipts, proofs, or release decisions.
