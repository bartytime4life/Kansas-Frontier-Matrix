# 🧪 Experiment Report Template — Python Environment (`requirements.txt`) 📦

📍 **Target file path**
```text
mcp/dev_prov/examples/10_experiment_report_template/(example_report_tree)/artifacts/manifest/environment/requirements.txt
```

## 🗂️ Expected placement
```text
📦 mcp/
└── dev_prov/
    └── examples/
        └── 10_experiment_report_template/
            └── (example_report_tree)/
                └── artifacts/
                    └── manifest/
                        └── environment/
                            └── requirements.txt  ✅
```

## ✅ `requirements.txt` (copy/paste)
```txt
# Kansas Frontier Matrix (KFM) — Experiment Environment Requirements
# Used by: dev_prov experiment report template (environment manifest)
#
# 🧩 Philosophy
# - Keep core installable across platforms
# - Use version ranges for template stability
# - For strict reproducibility, generate a pinned file via:
#   pip freeze > requirements.txt

# -----------------------------------------------------------------------------
# Core API / CLI / Config (FastAPI + Pydantic stack)
# -----------------------------------------------------------------------------
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0
python-dotenv>=1.0,<2.0

httpx>=0.27,<1.0
requests>=2.31,<3.0
orjson>=3.9,<4.0

rich>=13.7,<14.0
typer>=0.12,<1.0
jinja2>=3.1,<4.0
pyyaml>=6.0,<7.0
python-dateutil>=2.9,<3.0

# -----------------------------------------------------------------------------
# Auth / Security (token roles + API hardening)
# -----------------------------------------------------------------------------
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
cryptography>=42.0,<46.0

# -----------------------------------------------------------------------------
# Databases / Storage (PostGIS + Neo4j + object storage + cache)
# -----------------------------------------------------------------------------
sqlalchemy>=2.0,<3.0
geoalchemy2>=0.15,<1.0

psycopg2-binary>=2.9,<3.0
# asyncpg is optional (uncomment if you run async SQLAlchemy engines)
# asyncpg>=0.29,<1.0

neo4j>=5.0,<6.0
# neomodel is optional ORM-like modeling for Neo4j (uncomment if used)
# neomodel>=5.3,<6.0

redis>=5.0,<6.0

boto3>=1.34,<2.0
s3fs>=2024.6.0

# -----------------------------------------------------------------------------
# Catalog / Provenance / Governance (STAC/DCAT/PROV style workflows)
# -----------------------------------------------------------------------------
pystac>=1.10,<2.0
pystac-client>=0.8,<1.0
stac-pydantic>=3.0,<4.0

jsonschema>=4.22,<5.0
prov>=2.0,<3.0

rdflib>=7.0,<8.0
pyshacl>=0.25,<1.0
pyld>=2.0,<3.0

frictionless>=5.18,<6.0

# -----------------------------------------------------------------------------
# Geospatial / Remote Sensing / Analytics
# -----------------------------------------------------------------------------
numpy>=1.26,<3.0
pandas>=2.2,<3.0
pyarrow>=16.0,<19.0

geopandas>=0.14,<2.0
shapely>=2.0,<3.0
pyproj>=3.6,<4.0
fiona>=1.9,<2.0
rasterio>=1.3,<2.0
rtree>=1.2,<2.0

geojson>=3.1,<4.0
pyshp>=2.3,<3.0

scipy>=1.12,<2.0
pysal>=24.0,<25.0
scikit-learn>=1.4,<2.0
matplotlib>=3.8,<4.0

networkx>=3.2,<4.0

# GDAL python bindings can be OS-sensitive; prefer system packages when possible.
# Uncomment only if your build pipeline supports it.
# GDAL>=3.6

# -----------------------------------------------------------------------------
# Tiles / COG tooling (COG generation + dynamic tiling patterns)
# -----------------------------------------------------------------------------
rio-cogeo>=5.0,<6.0
rio-tiler>=6.0,<7.0
morecantile>=5.2,<6.0
mercantile>=1.2,<2.0
mapbox-vector-tile>=2.0,<3.0

# TiTiler is commonly installed via its namespace distributions.
# Uncomment the one you actually deploy.
# titiler.core>=0.20,<1.0
# titiler.application>=0.20,<1.0

# -----------------------------------------------------------------------------
# Pipelines / Scheduling (background tasks + scheduled ingests)
# -----------------------------------------------------------------------------
celery>=5.4,<6.0
apscheduler>=3.10,<4.0
rq>=1.16,<2.0

# -----------------------------------------------------------------------------
# Search / Metrics / Observability
# -----------------------------------------------------------------------------
elasticsearch>=8.0,<9.0
prometheus-client>=0.20,<1.0

# -----------------------------------------------------------------------------
# Document ingest (OCR + PDF parsing)
# -----------------------------------------------------------------------------
pdfplumber>=0.11,<1.0
pytesseract>=0.3,<1.0
Pillow>=10.0,<12.0

# -----------------------------------------------------------------------------
# NLP / AI (Focus Mode + auto-metadata / embeddings) — optional heavies
# -----------------------------------------------------------------------------
spacy>=3.7,<4.0
rapidfuzz>=3.8,<4.0
langdetect>=1.0,<2.0

openai>=1.0,<2.0
tiktoken>=0.7,<1.0

sentence-transformers>=3.0,<4.0
transformers>=4.41,<5.0

# GPU/ML stacks are intentionally NOT forced for the template.
# Uncomment ONE stack as needed:
# torch>=2.2
# tensorflow>=2.16
# fastai>=2.7

# Optional local vector store tooling:
# faiss-cpu>=1.8,<2.0
# chromadb>=0.5,<1.0

# -----------------------------------------------------------------------------
# Big-data / Lakehouse style tooling — optional
# -----------------------------------------------------------------------------
duckdb>=1.0,<2.0
polars>=1.0,<2.0
# dask[complete]>=2024.5.0
# pyspark>=3.5,<4.0

# Google Earth Engine tooling — optional
# earthengine-api>=0.1.400
# geemap>=0.33

# -----------------------------------------------------------------------------
# Notebooks / Reporting (useful for experiment writeups & artifacts)
# -----------------------------------------------------------------------------
ipython>=8.0,<9.0
jupyterlab>=4.2,<5.0
ipykernel>=6.29,<7.0
nbconvert>=7.16,<8.0

# -----------------------------------------------------------------------------
# Testing / Linting (CI + reproducibility discipline)
# -----------------------------------------------------------------------------
pytest>=8.2,<9.0
pytest-cov>=5.0,<6.0
hypothesis>=6.0,<7.0

ruff>=0.6,<1.0
black>=24.4,<25.0
mypy>=1.10,<2.0
pre-commit>=3.7,<4.0
```
