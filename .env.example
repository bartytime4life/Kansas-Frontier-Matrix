# 🔐 `.env.example` Template (Kansas-Matrix-System)

> [!IMPORTANT]
> ✅ Copy the `dotenv` block below into **`.env.example`** (repo root).  
> 🔒 Then copy **`.env.example` → `.env`** and put real secrets in **`.env`** (never commit `.env`).

## 📁 Recommended placement

```text
📦 repo-root/
├── 📄 .env.example   ✅ commit this
└── 🔒 .env           🚫 DO NOT commit
```

## 🧩 `.env.example` contents

```dotenv
#############################################
# Kansas Matrix System — .env.example 🔐
# Copy to `.env` and fill in your secrets.
#############################################

# =========================
# 🌐 Public / Canonical URLs
# =========================
# Used for link generation + UI config
KFM_BASE_URL=http://localhost:3000
KFM_API_BASE_URL=http://localhost:8000
# Example production pattern:
# KFM_BASE_URL=https://your-domain
# KFM_API_BASE_URL=https://your-domain/api

# =========================
# 🔑 Auth
# =========================
# ⚠️ CHANGE THIS in any real/shared environment
KFM_JWT_SECRET=change_me

# =========================
# 🧠 Backend (FastAPI/Uvicorn)
# =========================
FASTAPI_PORT=8000
KFM_API_RELOAD=true
KFM_API_WORKERS=1

# =========================
# 🧭 Frontend (React)
# =========================
WEB_PORT=3000

# In local dev:
REACT_APP_API_URL=http://localhost:8000
# If running inside Docker network, you may use:
# REACT_APP_API_URL=http://api:8000

# =========================
# 🗄️ Databases
# =========================
# PostGIS (PostgreSQL + PostGIS)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=kfm

# Neo4j (format: user/password)
NEO4J_AUTH=neo4j/neo4j

# =========================
# 🛡️ Policy (OPA)
# =========================
ENABLE_OPA=false

# =========================
# 🤖 AI / Focus Mode
# =========================
# Local Ollama (recommended for dev)
OLLAMA_API_URL=http://ollama:11434
FOCUS_MODE_MODEL=llama3.1
# Some setups use this name instead (kept for compatibility):
OLLAMA_MODEL=llama3.1

# Optional: external provider
OPENAI_API_KEY=
```