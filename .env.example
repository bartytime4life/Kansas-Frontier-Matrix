# Kansas Frontier Matrix (KFM) — .env.example
# Copy this file to `.env` and set real values for your environment.
# Do NOT commit `.env` (it contains secrets).

# ------------------------------------------------------------------------------
# API / Backend runtime
# ------------------------------------------------------------------------------
# Expected values commonly include: development | production
FASTAPI_ENV=development

# ------------------------------------------------------------------------------
# Neo4j (Graph) connection
# ------------------------------------------------------------------------------
# Connection details are managed via these env vars.
# If you run Neo4j locally:        bolt://localhost:7687
# If you run via docker-compose:   bolt://neo4j:7687  (service-name networking)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme
