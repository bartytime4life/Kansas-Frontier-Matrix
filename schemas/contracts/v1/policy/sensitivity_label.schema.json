{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.kfm.local/contracts/v1/policy/sensitivity_label.schema.json",
  "title": "sensitivity_label",
  "type": "object",
  "description": "KFM v1 contract schema stub for sensitivity_label. Greenfield placeholder; fields to be defined per the contract document and ADR.",
  "x-kfm": {
    "contract_doc": "contracts/policy/sensitivity_label.md",
    "fixtures_root": "fixtures/policy/sensitivity_label/",
    "validator": "tools/validators/policy/validate_sensitivity_label.py",
    "policy": "policy/policy/",
    "status": "PROPOSED"
  },
  "properties": {
    "spec_hash": {
      "type": "string",
      "description": "Deterministic content hash."
    },
    "id": {
      "type": "string",
      "description": "Canonical identifier."
    },
    "version": {
      "type": "string",
      "description": "Contract or object version."
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": true
}
