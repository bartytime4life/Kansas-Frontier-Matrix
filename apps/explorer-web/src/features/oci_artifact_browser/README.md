# OCI artifact browser

**Status:** PROPOSED_INACTIVE / fixture-only / unmounted
**Authority:** none

This Explorer feature renders an exact synthetic governed projection as a text-first review table of mutable tags, digest-pinned OCI identities, media types, sizes, recorded referrers, and one rollback candidate. It explicitly labels signatures as recorded but unverified and rollback as unauthorized.

The adapter enforces a closed shape, canonical UTC-second review time, digest/tag/reference coherence, unique tags and digests, subject-digest binding, deterministic referrer order, finite negative states with no registry detail, and six false governance flags. Malformed input renders nothing.

The feature is not mounted in a route. It performs no registry request, ORAS operation, signature verification, cryptography, credential handling, persistence, lifecycle mutation, rollback, promotion, release, deployment, publication, or public-use authorization.

Rollback is a focused revert of the adapter, feature, synthetic fixtures, tests, source map, and generated authoring receipt. No registry or released artifact is changed.
