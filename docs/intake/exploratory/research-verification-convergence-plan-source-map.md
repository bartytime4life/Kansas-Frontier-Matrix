# Research Verification Convergence Plan — Source Map

Status: **PROPOSED**, exploratory adaptation only.

This slice combines two supplied planning rules:

1. the *KFM Comprehensive Research and Verification Agenda* separates `EXT`, `REPO`, `DEC`, `STW`, and `TST` work, prioritizes it from `P0` through `P3`, and requires explicit closure evidence and residual unknowns; and
2. the *KFM Briefing-to-System Integration Architecture* recommends choosing at most one or two signals per convergence review so recurring discovery does not become an uncontrolled engineering campaign.

The repository object validates a human-proposed, capacity-bounded selection. It does not autonomously choose policy, owners, evidence, source activation, or public outcomes.

Placement follows accepted Directory Rules v2 and ADR-0029: meaning under `contracts/`, shape under `schemas/`, fixtures under `fixtures/`, validation under `tools/`, tests under `tests/`, and generated authoring provenance under `data/receipts/generated/`.
