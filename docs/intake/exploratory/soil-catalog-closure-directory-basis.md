# Soil catalog-closure assessment directory basis

Placement follows the responsibility roots already used by current Soil implementation and adopted Directory Rules: semantic meaning in `contracts/domains/soil/`, machine shape in `schemas/contracts/v1/domains/soil/`, fixtures in `fixtures/contracts/v1/domains/soil/`, validation in `tools/validators/domains/soil/`, tests in `tests/validators/domains/soil/`, workflow orchestration in `.github/workflows/`, human domain status in `docs/domains/soil/`, and exploratory adaptation notes in `docs/intake/exploratory/`.

No new repository root or competing schema, policy, evidence, catalog, release, or proof home is introduced.
