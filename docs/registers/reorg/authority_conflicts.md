# Authority Conflicts (CONFIRMED)
- CONFLICTED: `contracts/` and `schemas/` both contain schema-like machine artifacts; no move executed.
- CONFLICTED: `policy/` and `policies/` both exist; no machine policy files moved.
- CONFIRMED: this sprint moved docs only within `docs/domains/*` lanes.
