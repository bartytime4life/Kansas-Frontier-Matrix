# Authority Conflicts

- CONFLICTED: schema authority split across `contracts/`, `schemas/`, and `jsonschema/`; no machine-file moves performed.
- CONFLICTED: policy authority split across `policy/` and `policies/`; no rego/json policy moves performed.
- CONFIRMED: this sprint moved docs only and retained authority boundaries.
