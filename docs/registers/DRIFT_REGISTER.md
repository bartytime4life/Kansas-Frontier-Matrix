# DRIFT REGISTER
- 2026-05-09: PROPOSED shift: common/evidence/runtime schema placeholders replaced with first typed shapes.
- 2026-05-09: NEEDS VERIFICATION PR-001 `$id` URL-to-file-path inspection found zero mismatches under `schemas/contracts/v1/**/*.schema.json`.

- 2026-05-12: CONFIRMED blocking drift: required doctrine artifacts for evidence-first comparison are not present in mounted repo snapshot (`KFM_Pass_18_Idea_Index_Category_Atlas_and_Expansion_Dossier.pdf`, `Master_MapLibre_Components-Functions-Features.pdf`, `Kansas_Frontier_Matrix_Definitive_Greenfield_Building_Plan_v1_1.pdf`); impact: doctrine-vs-implementation gap analysis cannot be completed from primary sources; resolution path: add canonical artifacts (or approved canonical links with provenance) and re-run extraction.

- 2026-05-12: CONFIRMED remediation staged: added governance gate scaffolding for required doctrine artifacts (`control_plane/document_registry_doctrine_required.yaml`, `policy/source/doctrine_artifact_required.rego`, schema+fixtures+tests+validator script). Blocking status remains until artifacts themselves are admitted.
- 2026-05-13: CONFIRMED blocker resolved: required doctrine artifact filenames now exist under `docs/doctrine/artifacts/` and `control_plane/document_registry_doctrine_required.yaml` statuses have been updated to `present`; doctrine prerequisite gate can now be executed in pass mode.
- 2026-05-13: CONFIRMED integrity drift: admitted doctrine artifacts currently fail minimum-integrity checks (tiny files and duplicate hashes), so doctrine extraction remains blocked until canonical artifacts or canonical linked provenance records are supplied.
- 2026-05-13: CONFIRMED correction: placeholder doctrine PDFs were removed and registry statuses reset to `missing`; doctrine gate remains blocked pending canonical artifact admission or approved canonical-link provenance records.
- 2026-05-13: CONFIRMED provenance drift: current doctrine provenance registry uses placeholder `example.org` source URLs; canonical-link admission path remains blocked until real authoritative source URLs are recorded.
- 2026-05-15: CONFIRMED naming drift surfaced in Wave 1 discovery: Directory Rules references `docs/registers/AUTHORITY_LADDER.md` and `docs/registers/OBJECT_FAMILY_MAP.md`, while repository prior state used `docs/registers/OBJECT_FAMILY.md` plus only YAML authority ladder in `control_plane/authority_ladder.yaml`; Wave 1 added PROPOSED Markdown scaffolds for naming parity pending steward confirmation.
- 2026-05-15: CONFIRMED artifact-path drift for required inputs in this run: doctrine filenames used spaces/case variants (`docs/DomainDriven Design Reference.pdf`, `docs/New Ideas 5-8-26.pdf`) and one atlas delivered as Markdown (`docs/KFM_Domains_Culmination_Atlas_v1_1.md`) instead of the exact required PDF path names; run proceeded using explicitly approved substitutes.

- 2026-05-15: CONFIRMED evidence-claim drift: `control_plane/normalized_summary_consumer_readiness.yaml` previously marked consumer checks as `validated` with specific evidence labels; this run demoted those entries to `needs_verification`/`UNKNOWN` pending executable proof in current branch context.

- 2026-05-15: CONFIRMED provenance-placeholder drift remediated: `control_plane/doctrine_artifact_provenance_sources.yaml` no longer carries synthetic `example.org` links; `source_url` values set to `NEEDS_VERIFICATION` until steward-approved authoritative URLs are admitted.

- 2026-05-15: CONFIRMED status-normalization drift remediated: `control_plane/document_registry_doctrine_required.yaml` artifact states changed from `missing` to `needs_verification` to distinguish absent evidence from unresolved canonical-path/provenance validation.

- 2026-05-15: CONFIRMED execution drift: in-branch run of `pytest -q tests/policy/test_preflight_summary_consistency.py` failed (2 failed / 3 passed) with `render_returncode: 2` and `render_stderr: skipped_due_to_check_error`; readiness state for the mapped consumer remains non-validated.

- 2026-05-15: CONFIRMED readiness-state reconciliation: `control_plane/normalized_summary_consumer_readiness.yaml` now reflects branch-local outcomes (`passing` for doctrine artifact suite, `failing` for policy preflight consistency) instead of blanket `needs_verification` placeholders.
- 2026-05-16: CONFIRMED parser-compatibility drift remediated: `scripts/maintenance/_doctrine_registry.py` now accepts `needs_verification` in `required_doctrine_artifacts` statuses, unblocking preflight summary generation and downstream policy consistency tests.
- 2026-05-16: CONFIRMED required-input path drift for Wave 1 bootstrap run: one doctrine input was missing at the required type/path (`KFM_Domains_Culmination_Atlas_v1_1.pdf` present only as Markdown: `docs/KFM_Domains_Culmination_Atlas_v1_1.md`), and two required inputs existed only at spaced-path aliases (`docs/New Ideas 5-8-26.pdf`, `docs/DomainDriven Design Reference.pdf`) rather than underscore-path names (`New_Ideas_5-8-26.pdf`, `DomainDriven_Design_Reference.pdf`); operator authorized explicit skip/proceed; this was a canonical-path/name governance drift (not a claim that source content was absent), and corroboration confidence remained reduced pending canonical mapping and provenance decisions.
- 2026-05-17: CONFIRMED re-check drift state (superseded by target-name clarification below): repository inventory still shows `docs/KFM_Domains_Culmination_Atlas_v1_1.md`, `docs/New Ideas 5-8-26.pdf`, and `docs/DomainDriven Design Reference.pdf`, with no underscore-path canonical counterparts; drift remains OPEN pending steward-approved canonicalization/provenance mapping.
- 2026-05-17: CONFIRMED canonical-name target clarification: unresolved canonical names under review are `KFM_Domains_Culmination_Atlas_v1_1.pdf`, `New_Ideas_5-8-26.pdf`, and `DomainDriven_Design_Reference.pdf`; current repository paths remain alias/format variants pending steward decision.

- 2026-07-22: CONFIRMED artifacts authority drift at `main@86c039f9fda31eb2d4a75112911a5c400ae93a16`: the tracked 44-file `artifacts/` compatibility root contains the nonconforming `artifacts/release/` lane and a `PROPOSED` `artifacts/release/people-dna-land/release_manifest.json` placeholder, while the Makefile, MapLibre scripts, and `maplibre-perf-governance` workflow generate receipt-, proof-, correction-, rollback-, and release-shaped staging files under untracked `artifacts/perf/`. These paths are not canonical trust objects and do not evidence release, promotion, correction, rollback, or publication. Status: OPEN / `BLOCKED_ADR` under Directory Rules `OPEN-DR-09-b` and the MapLibre artifact-placement conflict. Resolution requires an accepted artifacts/release-placement decision and a reviewed one-pass migration with rollback; canonical receipts, proofs, release decisions, catalogs, and published outputs remain under `data/receipts/`, `data/proofs/`, `release/`, `data/catalog/`, and `data/published/`.

- 2026-09-26: CONFIRMED unlogged placement conflict — crosswalk validator home (`ADR-S-CWV-01`), first surfaced in `docs/domains/hydrology/IDENTITY_MODEL.md`, `FILE_SYSTEM_PLAN.md` (HYD FS plan), `EXPANSION_BACKLOG.md` (item `HYD-H05`), and `EXPANSION_PLAN.md`, each of which instructs "do not assert one [home] until the ADR lands; log a DRIFT_REGISTER entry" but none had done so. The corpus proposes three competing homes for the COMID ↔ HUC12 crosswalk validator (`compute_crosswalk`/`score_alignment`/`verify_manifest`): `tools/probes/comid_huc12/` (Atlas crosswalk card KFM-P5-PROG-0008), `tools/validators/validators/crosswalk/` (Doctrine Synthesis worked example), and a flat `tools/validators/hydro/` (earlier hydrology planning). Repo-evidence at this snapshot: `tools/validators/hydro/check_crosswalk.py`, `check_huc12.py`, `emit_manifest.py`, and `test_check_crosswalk.py` exist only as one-line `PROPOSED placeholder` stubs at the third (flat, unratified) candidate home; no `docs/adr/` file for `ADR-S-CWV-01` exists yet, and the companion `comid_huc12_crosswalk.schema.json` home (`schemas/contracts/v1/domains/hydrology/` vs. `schemas/contracts/v1/crosswalks/`) is likewise still open. Impact: any implementation added under `tools/validators/hydro/` before the ADR lands risks asserting an unratified home and creating a second migration later. Resolution path: raise `ADR-S-CWV-01` to decide the validator (and paired schema) home, then implement the deterministic offline COMID↔HUC12 validator only at the accepted path; until then this entry is the required drift log and the placeholder stubs must not be treated as an implicit placement decision.

- 2026-09-26: CONFIRMED unlogged reconciliation gap — `ADR-HAZ-08`, surfaced in `docs/domains/hazards/MISSING_OR_PLANNED_FILES.md` ("Reconcile Atlas §24.13 crosswalk vs. §12 segment form | See ADR-HAZ-08; log a DRIFT_REGISTER entry | CONFLICTED"), had not been logged here. No `docs/adr/` file for `ADR-HAZ-08` exists yet. Impact: the hazards domain has two undecided candidate shapes (an Atlas §24.13 crosswalk form and a §12 segment form) for the same construct, alongside the still-`BLOCKED-ON-ADR` `HazardsDecisionEnvelope` schema home (ADR-HAZ-01), NFHL attribute-preservation contract location (ADR-HAZ-03), and release-gate `.rego` home (ADR-HAZ-07); building hazards pipeline or validator code against either shape now risks rework once the ADR lands. Resolution path: raise `ADR-HAZ-08` to pick one form (or a documented mapping between both) before hazards crosswalk implementation proceeds.

- 2026-09-26: CONFIRMED placement conflict at the placeholder stage — `tools/scripts/aggregate.py` is a one-line `PROPOSED placeholder` (docstring: "created from docs/domains/agriculture/FILE_SYSTEM_PLAN.md for tools/scripts/aggregate.py"), but that same plan's §9 "What does **not** go here" table names this exact path as a rejected placement: "Agriculture county-aggregation helper in `tools/scripts/aggregate.py` (one-off) — Long-lived trust-bearing helpers must graduate — Correct home (PROPOSED): `tools/` (if cross-cutting), `packages/domains/agriculture/` (if reusable), or `pipelines/domains/agriculture/` (if a workflow step)." §9 also states reviewers MUST reject such placements "absent an accepted ADR or migration note," and no ADR or migration note choosing among the three candidate homes exists yet; no test or fixture pins `tools/scripts/aggregate.py`'s interface either, so the path is unratified on both counts. Impact: implementing the county-aggregation helper in place at `tools/scripts/aggregate.py` would itself be the drift the plan warns against — the placeholder's current path must not be read as an implicit placement decision. Resolution path: decide (ADR or migration note) whether the helper is cross-cutting (`tools/`), reusable (`packages/domains/agriculture/`), or a single workflow step (`pipelines/domains/agriculture/`), then implement only at the accepted path.

- 2026-09-26: CONFIRMED unlogged placement conflict — genealogy validator lane vs. the People/DNA/Land whole-domain segment (`OQ-PEOPLE-SUB-01`/`OQ-PEOPLE-SUB-02`). `docs/domains/people-dna-land/sublanes/genealogy.md` §14.1 names `tools/validators/people-dna-land/validate_gedcom.py`, `.../validate_consent_receipt.py`, and `.../validate_overlay_pointer.py` as the PROPOSED validator homes, with an explicit note that "[a] flat `tools/validators/genealogy/` home (used in an earlier draft) would presuppose the unresolved sublane partition" per Directory Rules §12 (whole-domain lanes only; no sublane subdivision for schemas/policy/tests/tooling). The same document's §16 open-questions table closes with "Items that propose new placements or conventions — and the two CONFLICTED rows above [`OQ-PEOPLE-SUB-02`, `OQ-GEN-12`] — will additionally appear in `docs/registers/DRIFT_REGISTER.md`," but no such entry existed here until now. Repo-evidence at this snapshot already contradicts the doc's own rejection: `tools/validators/genealogy/` exists and carries one executable, tested validator (`screen_living_persons.py`, 121 lines, delegating to `validate_historical_person_place_event_resolution`, backed by `tests/validators/test_screen_living_persons.py`) plus three still-unimplemented one-line `PROPOSED placeholder` stubs at that same rejected segment (`validate_gedcom.py`, `validate_consent_receipt.py`, `validate_overlay_pointer.py`, each generated from this exact sublane doc). `tools/validators/genealogy/README.md` itself acknowledges "an unresolved sublane-placement ADR question" without naming an ADR id, and no `docs/adr/` file resolves `OQ-PEOPLE-SUB-01` or `OQ-PEOPLE-SUB-02` yet. Impact: the three placeholder validators cannot be implemented in place without either (a) implicitly ratifying the already-precedented `genealogy/` segment, contradicting the sublane doc's explicit rejection, or (b) migrating the existing `screen_living_persons.py` and its test off that segment first; either way the choice is a placement decision the plan says must not be made silently. Resolution path: raise an ADR (or extend `ADR-0029`/Directory Rules §12) settling whether `tools/validators/` subdivides People/DNA/Land tooling by sublane; until accepted, do not read `tools/validators/genealogy/`'s existing contents as an implicit placement decision, and implement the three pending validators only at whichever path the ADR settles.

- 2026-09-26: CONFIRMED partial resolution of the above entry — `ADR-0042` was raised (proposed, registered in `docs/adr/INDEX.md`) recommending `tools/validators/domains/people-dna-land/` over either candidate named above: it is the already-established, already-populated convention already used by the 13 registered core domains (agriculture, archaeology, atmosphere, fauna, flora, geology, habitat, hazards, hydrology, people-dna-land, roads-rail-trade, settlements-infrastructure, soil) plus one additional confirmed-implementation lane, `water_planning`, making 14 populated lanes under `tools/validators/domains/` at this snapshot. `tools/validators/domains/people-dna-land/` independently already carries substantive validators (`validate_consent_overlay.py`, `validate_consent_revocation_propagation_assessment.py`) overlapping two of the three pending stubs. `ADR-0042` remains `proposed`; acceptance and the described migration of `screen_living_persons.py` are still pending, so the underlying conflict is not yet closed.

- 2026-09-26: CONFIRMED unverifiable placeholder provenance — `tools/validators/validate_attestation_ref.py` is a one-line `PROPOSED placeholder` whose docstring claims it was "created from docs/domains/hazards/FILE_SYSTEM_PLAN.md," but that document contains no mention of `attestation_ref`, `validate_attestation_ref`, or any validator at this or a related path. Its only "attestation" reference is an unrelated storage-location row, "Signed attestations | `release/signatures/<release_id>/` | release authority | PROPOSED" (§ release-lane table), which names where signed-attestation *artifacts* would live, not a validator for an `attestation_ref` *field*. The `attestation_refs[]` field this validator presumably targets is real and repo-wide (`docs/standards/RELEASE_MANIFEST.md`, `docs/architecture/release-model.md`, `docs/architecture/release-discipline.md` §13.4–13.5, `contracts/release/cosign_attestation_verification_plan.md`), and its shared, non-domain-nested path (`tools/validators/validate_attestation_ref.py`, not under `tools/validators/domains/hazards/`) is itself consistent with those cross-cutting, ReleaseManifest-level documents rather than with hazards specifically — but none of them proposes this exact filename or path either, so no document currently grounds this placeholder's existence at this path under any name. Impact: the placeholder's own citation cannot be used to justify implementing it in place, and its correct owning document/root is unresolved (candidates include a shared release-authority document, not `docs/domains/hazards/`). Resolution path: identify (or write) the document that actually proposes an `attestation_refs[]` verifier, correct or add the citation, and confirm the path against that document's proposal before implementing; until then this citation must not be read as an accepted placement.

- 2026-09-26: CONFIRMED placement conflict — `policy/genealogy/publication.rego` vs. `policy/domains/people-dna-land/`, the policy-root sibling of the already-logged `tools/validators/genealogy/` conflict above. The four-line `PROPOSED` scaffold's own header cites `docs/domains/people-dna-land/sublanes/dna.md` as its source; that document's own domain-lane table (line 381) names `policy/domains/people-dna-land/` as the "(PROPOSED — §12 lane form)" home, not a flat `policy/genealogy/` segment. `policy/domains/people-dna-land/` already carries six real `.rego` files (`abstain_on_ambiguous.rego`, `consent_validator.rego`, `deny_unpublished.rego`, `dna_restricted.rego`, `living_person.rego`, `living_person_redaction.rego`, plus `consent/dna_consent_revocation.rego`) and an already-reserved, still-empty `policy/domains/people-dna-land/genealogy/.gitkeep` subdirectory, while `policy/genealogy/` holds only its own README and this one scaffold. `policy/genealogy/README.md` (v0.2, 561 lines) already self-labels its own status `placement-conflicted` and its `truth_posture` field explicitly names "CONFLICTED policy/genealogy/ compatibility path versus policy/domains/people-dna-land/ domain placement," but the README does not instruct logging here and no DRIFT_REGISTER entry existed until now. Impact: same shape as the tools/validators sibling — implementing `publication.rego` in place would ratify an already-self-flagged-conflicted path over the domain doc's own named alternative and the already-reserved `genealogy/` subdirectory. Resolution path: `ADR-0043` (raised alongside this entry) proposes resolving this the same way `ADR-0042` resolved the tools/validators case.

- 2026-09-26: CONFIRMED stale placeholder under an explicitly retired name — `policy/domains/fauna/api_envelope.rego` is a live four-line `PROPOSED` scaffold, but `docs/domains/fauna/MISSING_OR_PLANNED_FILES.md` (line 368, reconciliation log line 613) states this exact file/construct "was `api_envelope.rego` / `FaunaDecisionEnvelope` — reconciled to `RuntimeResponseEnvelope` / `runtime_envelope.rego`... bespoke envelope retired," naming `policy/domains/fauna/runtime_envelope.rego` as the replacement. `policy/domains/fauna/runtime_envelope.rego` does not exist in the repository at this snapshot — only the doc-declared-retired `api_envelope.rego` does. Impact: a future implementer following the doc's own current table would build `runtime_envelope.rego`, but a naive read of the repository tree would find and could extend the already-retired `api_envelope.rego` instead, reviving a name the domain's own reconciliation note says is gone. Resolution path: either rename the existing scaffold to `runtime_envelope.rego` per the doc's own reconciliation (a mechanical, already-decided rename, not a new placement decision) or delete it and create `runtime_envelope.rego` fresh; either way, do not implement real logic under the retired `api_envelope.rego` name.

- 2026-09-26: CONFIRMED cross-domain placement inconsistency — `policy/domains/<domain>/` vs. `policy/sensitivity/<domain>/` for domain sensitivity-policy scaffolds, confirmed in three domains with inconsistent direction, none previously logged here. (1) Archaeology: `docs/domains/archaeology/MISSING_OR_PLANNED_FILES.md` proposes `policy/sensitivity/archaeology/looting_risk_deny.rego`, `collection_security_deny.rego`, `sacred_site_deny.rego`, and `exact_location_deny.rego` (lines 365-369), but the actual four-line `PROPOSED` scaffolds for all four (plus `burial_and_human_remains_deny.rego`) exist at `policy/domains/archaeology/` instead; `policy/sensitivity/archaeology/` currently holds only `sovereignty_chip_required.rego` and no README, a state `policy/domains/archaeology/sensitivity/README.md` already independently confirmed ("`policy/sensitivity/archaeology/README.md` was not found at the checked path"). (2) Flora: `docs/domains/flora/FILE_SYSTEM_PLAN.md` proposes `policy/domains/flora/rare_plant_geoprivacy.rego`, `join_induced_sensitivity.rego`, `cultural_sensitivity.rego`, and `exact_geometry_deny.rego` (lines 759-762), but the actual four-line scaffolds exist at `policy/sensitivity/flora/` instead — the opposite direction from the archaeology case. `policy/domains/flora/README.md` (line 99) separately states the two lanes should "compose independent controls; do not collapse sensitivity into general domain policy," and already carries its own open, unresolved item on this exact tension (`FLORAPOL-003`, "CONFLICTED / NEEDS ADR OR MIGRATION"), so the FILE_SYSTEM_PLAN.md table may itself be the stale side here rather than the actual file placement. (3) People/DNA/Land: `docs/domains/people-dna-land/DATA_LIFECYCLE.md` (line 552) names `policy/sensitivity/people/living_person.rego` (PROPOSED) as settling living-person policy specifics, but the actual four-line scaffold is `policy/domains/people-dna-land/living_person.rego`; neither `policy/sensitivity/people/` nor `policy/sensitivity/people-dna-land/living_person.rego` exists, though a reserved, empty `policy/sensitivity/people-dna-land/living_person/.gitkeep` directory does, suggesting a third candidate shape (a subdirectory, not a flat file) nobody has chosen either. Impact: at least three domains disagree, in different directions, about which root owns generated sensitivity-policy scaffolds, and Flora's own README already treats this as a live open question without an accepted answer; implementing any of these nine pending scaffolds in place risks the same silent-ratification problem as the other entries here. Resolution path: this is broader than a single-file placement decision and needs its own cross-domain ADR (or an amendment to `ADR-0029`/Directory Rules §12) settling whether `policy/sensitivity/<domain>/` is a distinct, composable lane (as Flora's README already asserts) or should collapse into `policy/domains/<domain>/`, and reconciling each domain's planning doc to match; not attempted in this entry.
