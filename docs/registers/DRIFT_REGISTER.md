# DRIFT REGISTER
- 2026-05-09: PROPOSED shift: common/evidence/runtime schema placeholders replaced with first typed shapes.
- 2026-05-09: NEEDS VERIFICATION PR-001 `$id` URL-to-file-path inspection found zero mismatches under `schemas/contracts/v1/**/*.schema.json`.

- 2026-05-12: CONFIRMED blocking drift: required doctrine artifacts for evidence-first comparison are not present in mounted repo snapshot (`KFM_Pass_18_Idea_Index_Category_Atlas_and_Expansion_Dossier.pdf`, `Master_MapLibre_Components-Functions-Features.pdf`, `Kansas_Frontier_Matrix_Definitive_Greenfield_Building_Plan_v1_1.pdf`); impact: doctrine-vs-implementation gap analysis cannot be completed from primary sources; resolution path: add canonical artifacts (or approved canonical links with provenance) and re-run extraction.

- 2026-05-12: CONFIRMED remediation staged: added governance gate scaffolding for required doctrine artifacts (`control_plane/document_registry_doctrine_required.yaml`, `policy/source/doctrine_artifact_required.rego`, schema+fixtures+tests+validator script). Blocking status remains until artifacts themselves are admitted.
- 2026-05-13: CONFIRMED blocker resolved: required doctrine artifact filenames now exist under `docs/doctrine/artifacts/` and `control_plane/document_registry_doctrine_required.yaml` statuses have been updated to `present`; doctrine prerequisite gate can now be executed in pass mode.
- 2026-05-13: CONFIRMED integrity drift: admitted doctrine artifacts currently fail minimum-integrity checks (tiny files and duplicate hashes), so doctrine extraction remains blocked until canonical artifacts or canonical linked provenance records are supplied.
- 2026-05-13: CONFIRMED correction: placeholder doctrine PDFs were removed and registry statuses reset to `missing`; doctrine gate remains blocked pending canonical artifact admission or approved canonical-link provenance records.
- 2026-05-13: CONFIRMED provenance drift: current doctrine provenance registry uses placeholder `example.org` source URLs; canonical-link admission path remains blocked until real authoritative source URLs are recorded.
- 2026-05-15: CONFIRMED naming drift surfaced in Wave 1 discovery: Directory Rules references `docs/registers/AUTHORITY_LADDER.md` and `docs/registers/OBJECT_FAMILY_MAP.md`, while repository prior state used `docs/registers/OBJECT_FAMILY.md` plus only YAML authority ladder in `control_plane/authority_ladder.yaml`; Wave 1 added PROPOSED Markdown scaffolds for naming parity pending steward confirmation.
- 2026-05-15: CONFIRMED artifact-path drift for required inputs in this run: doctrine filenames used spaces/case variants (`docs/DomainDriven Design Reference.pdf`, `docs/New Ideas 5-8-26.pdf`) and one atlas delivered as Markdown (`docs/KFM_Domains_Culmination_Atlas_v1_1.md`) instead of the exact required PDF path names; run proceeded using explicitly approved substitutes.

- 2026-05-15: CONFIRMED evidence-claim drift: `control_plane/normalized_summary_consumer_readiness.yaml` previously marked consumer checks as `validated` with specific evidence labels; this run demoted those entries to `needs_verification`/`UNKNOWN` pending executable proof in current branch context.

- 2026-05-15: CONFIRMED provenance-placeholder drift remediated: `control_plane/doctrine_artifact_provenance_sources.yaml` no longer carries synthetic `example.org` links; `source_url` values set to `NEEDS_VERIFICATION` until steward-approved authoritative URLs are admitted.

- 2026-05-15: CONFIRMED status-normalization drift remediated: `control_plane/document_registry_doctrine_required.yaml` artifact states changed from `missing` to `needs_verification` to distinguish absent evidence from unresolved canonical-path/provenance validation.

- 2026-05-15: CONFIRMED execution drift: in-branch run of `pytest -q tests/policy/test_preflight_summary_consistency.py` failed (2 failed / 3 passed) with `render_returncode: 2` and `render_stderr: skipped_due_to_check_error`; readiness state for the mapped consumer remains non-validated.

- 2026-05-15: CONFIRMED readiness-state reconciliation: `control_plane/normalized_summary_consumer_readiness.yaml` now reflects branch-local outcomes (`passing` for doctrine artifact suite, `failing` for policy preflight consistency) instead of blanket `needs_verification` placeholders.
- 2026-05-16: CONFIRMED parser-compatibility drift remediated: `scripts/maintenance/_doctrine_registry.py` now accepts `needs_verification` in `required_doctrine_artifacts` statuses, unblocking preflight summary generation and downstream policy consistency tests.
- 2026-05-16: CONFIRMED required-input path drift for Wave 1 bootstrap run: one doctrine input was missing at the required type/path (`KFM_Domains_Culmination_Atlas_v1_1.pdf` present only as Markdown: `docs/KFM_Domains_Culmination_Atlas_v1_1.md`), and two required inputs existed only at spaced-path aliases (`docs/New Ideas 5-8-26.pdf`, `docs/DomainDriven Design Reference.pdf`) rather than underscore-path names (`New_Ideas_5-8-26.pdf`, `DomainDriven_Design_Reference.pdf`); operator authorized explicit skip/proceed; this was a canonical-path/name governance drift (not a claim that source content was absent), and corroboration confidence remained reduced pending canonical mapping and provenance decisions.
- 2026-05-17: CONFIRMED re-check drift state (superseded by target-name clarification below): repository inventory still shows `docs/KFM_Domains_Culmination_Atlas_v1_1.md`, `docs/New Ideas 5-8-26.pdf`, and `docs/DomainDriven Design Reference.pdf`, with no underscore-path canonical counterparts; drift remains OPEN pending steward-approved canonicalization/provenance mapping.
- 2026-05-17: CONFIRMED canonical-name target clarification: unresolved canonical names under review are `KFM_Domains_Culmination_Atlas_v1_1.pdf`, `New_Ideas_5-8-26.pdf`, and `DomainDriven_Design_Reference.pdf`; current repository paths remain alias/format variants pending steward decision.

- 2026-07-22: CONFIRMED artifacts authority drift at `main@86c039f9fda31eb2d4a75112911a5c400ae93a16`: the tracked 44-file `artifacts/` compatibility root contains the nonconforming `artifacts/release/` lane and a `PROPOSED` `artifacts/release/people-dna-land/release_manifest.json` placeholder, while the Makefile, MapLibre scripts, and `maplibre-perf-governance` workflow generate receipt-, proof-, correction-, rollback-, and release-shaped staging files under untracked `artifacts/perf/`. These paths are not canonical trust objects and do not evidence release, promotion, correction, rollback, or publication. Status: OPEN / `BLOCKED_ADR` under Directory Rules `OPEN-DR-09-b` and the MapLibre artifact-placement conflict. Resolution requires an accepted artifacts/release-placement decision and a reviewed one-pass migration with rollback; canonical receipts, proofs, release decisions, catalogs, and published outputs remain under `data/receipts/`, `data/proofs/`, `release/`, `data/catalog/`, and `data/published/`.

- 2026-09-26: CONFIRMED unlogged placement conflict — crosswalk validator home (`ADR-S-CWV-01`), first surfaced in `docs/domains/hydrology/IDENTITY_MODEL.md`, `FILE_SYSTEM_PLAN.md` (HYD FS plan), `EXPANSION_BACKLOG.md` (item `HYD-H05`), and `EXPANSION_PLAN.md`, each of which instructs "do not assert one [home] until the ADR lands; log a DRIFT_REGISTER entry" but none had done so. The corpus proposes three competing homes for the COMID ↔ HUC12 crosswalk validator (`compute_crosswalk`/`score_alignment`/`verify_manifest`): `tools/probes/comid_huc12/` (Atlas crosswalk card KFM-P5-PROG-0008), `tools/validators/validators/crosswalk/` (Doctrine Synthesis worked example), and a flat `tools/validators/hydro/` (earlier hydrology planning). Repo-evidence at this snapshot: `tools/validators/hydro/check_crosswalk.py`, `check_huc12.py`, `emit_manifest.py`, and `test_check_crosswalk.py` exist only as one-line `PROPOSED placeholder` stubs at the third (flat, unratified) candidate home; no `docs/adr/` file for `ADR-S-CWV-01` exists yet, and the companion `comid_huc12_crosswalk.schema.json` home (`schemas/contracts/v1/domains/hydrology/` vs. `schemas/contracts/v1/crosswalks/`) is likewise still open. Impact: any implementation added under `tools/validators/hydro/` before the ADR lands risks asserting an unratified home and creating a second migration later. Resolution path: raise `ADR-S-CWV-01` to decide the validator (and paired schema) home, then implement the deterministic offline COMID↔HUC12 validator only at the accepted path; until then this entry is the required drift log and the placeholder stubs must not be treated as an implicit placement decision.

- 2026-09-26: CONFIRMED unlogged reconciliation gap — `ADR-HAZ-08`, surfaced in `docs/domains/hazards/MISSING_OR_PLANNED_FILES.md` ("Reconcile Atlas §24.13 crosswalk vs. §12 segment form | See ADR-HAZ-08; log a DRIFT_REGISTER entry | CONFLICTED"), had not been logged here. No `docs/adr/` file for `ADR-HAZ-08` exists yet. Impact: the hazards domain has two undecided candidate shapes (an Atlas §24.13 crosswalk form and a §12 segment form) for the same construct, alongside the still-`BLOCKED-ON-ADR` `HazardsDecisionEnvelope` schema home (ADR-HAZ-01), NFHL attribute-preservation contract location (ADR-HAZ-03), and release-gate `.rego` home (ADR-HAZ-07); building hazards pipeline or validator code against either shape now risks rework once the ADR lands. Resolution path: raise `ADR-HAZ-08` to pick one form (or a documented mapping between both) before hazards crosswalk implementation proceeds.

- 2026-09-26: CONFIRMED placement conflict at the placeholder stage — `tools/scripts/aggregate.py` is a one-line `PROPOSED placeholder` (docstring: "created from docs/domains/agriculture/FILE_SYSTEM_PLAN.md for tools/scripts/aggregate.py"), but that same plan's §9 "What does **not** go here" table names this exact path as a rejected placement: "Agriculture county-aggregation helper in `tools/scripts/aggregate.py` (one-off) — Long-lived trust-bearing helpers must graduate — Correct home (PROPOSED): `tools/` (if cross-cutting), `packages/domains/agriculture/` (if reusable), or `pipelines/domains/agriculture/` (if a workflow step)." §9 also states reviewers MUST reject such placements "absent an accepted ADR or migration note," and no ADR or migration note choosing among the three candidate homes exists yet; no test or fixture pins `tools/scripts/aggregate.py`'s interface either, so the path is unratified on both counts. Impact: implementing the county-aggregation helper in place at `tools/scripts/aggregate.py` would itself be the drift the plan warns against — the placeholder's current path must not be read as an implicit placement decision. Resolution path: decide (ADR or migration note) whether the helper is cross-cutting (`tools/`), reusable (`packages/domains/agriculture/`), or a single workflow step (`pipelines/domains/agriculture/`), then implement only at the accepted path.

- 2026-09-26: CONFIRMED unlogged placement conflict — genealogy validator lane vs. the People/DNA/Land whole-domain segment (`OQ-PEOPLE-SUB-01`/`OQ-PEOPLE-SUB-02`). `docs/domains/people-dna-land/sublanes/genealogy.md` §14.1 names `tools/validators/people-dna-land/validate_gedcom.py`, `.../validate_consent_receipt.py`, and `.../validate_overlay_pointer.py` as the PROPOSED validator homes, with an explicit note that "[a] flat `tools/validators/genealogy/` home (used in an earlier draft) would presuppose the unresolved sublane partition" per Directory Rules §12 (whole-domain lanes only; no sublane subdivision for schemas/policy/tests/tooling). The same document's §16 open-questions table closes with "Items that propose new placements or conventions — and the two CONFLICTED rows above [`OQ-PEOPLE-SUB-02`, `OQ-GEN-12`] — will additionally appear in `docs/registers/DRIFT_REGISTER.md`," but no such entry existed here until now. Repo-evidence at this snapshot already contradicts the doc's own rejection: `tools/validators/genealogy/` exists and carries one executable, tested validator (`screen_living_persons.py`, 121 lines, delegating to `validate_historical_person_place_event_resolution`, backed by `tests/validators/test_screen_living_persons.py`) plus three still-unimplemented one-line `PROPOSED placeholder` stubs at that same rejected segment (`validate_gedcom.py`, `validate_consent_receipt.py`, `validate_overlay_pointer.py`, each generated from this exact sublane doc). `tools/validators/genealogy/README.md` itself acknowledges "an unresolved sublane-placement ADR question" without naming an ADR id, and no `docs/adr/` file resolves `OQ-PEOPLE-SUB-01` or `OQ-PEOPLE-SUB-02` yet. Impact: the three placeholder validators cannot be implemented in place without either (a) implicitly ratifying the already-precedented `genealogy/` segment, contradicting the sublane doc's explicit rejection, or (b) migrating the existing `screen_living_persons.py` and its test off that segment first; either way the choice is a placement decision the plan says must not be made silently. Resolution path: raise an ADR (or extend `ADR-0029`/Directory Rules §12) settling whether `tools/validators/` subdivides People/DNA/Land tooling by sublane; until accepted, do not read `tools/validators/genealogy/`'s existing contents as an implicit placement decision, and implement the three pending validators only at whichever path the ADR settles.

- 2026-09-26: CONFIRMED partial resolution of the above entry — `ADR-0042` was raised (proposed, registered in `docs/adr/INDEX.md`) recommending `tools/validators/domains/people-dna-land/` over either candidate named above: it is the already-established, already-populated convention already used by the 13 registered core domains (agriculture, archaeology, atmosphere, fauna, flora, geology, habitat, hazards, hydrology, people-dna-land, roads-rail-trade, settlements-infrastructure, soil) plus one additional confirmed-implementation lane, `water_planning`, making 14 populated lanes under `tools/validators/domains/` at this snapshot. `tools/validators/domains/people-dna-land/` independently already carries substantive validators (`validate_consent_overlay.py`, `validate_consent_revocation_propagation_assessment.py`) overlapping two of the three pending stubs. `ADR-0042` remains `proposed`; acceptance and the described migration of `screen_living_persons.py` are still pending, so the underlying conflict is not yet closed.

- 2026-09-26: CONFIRMED unverifiable placeholder provenance — `tools/validators/validate_attestation_ref.py` is a one-line `PROPOSED placeholder` whose docstring claims it was "created from docs/domains/hazards/FILE_SYSTEM_PLAN.md," but that document contains no mention of `attestation_ref`, `validate_attestation_ref`, or any validator at this or a related path. Its only "attestation" reference is an unrelated storage-location row, "Signed attestations | `release/signatures/<release_id>/` | release authority | PROPOSED" (§ release-lane table), which names where signed-attestation *artifacts* would live, not a validator for an `attestation_ref` *field*. The `attestation_refs[]` field this validator presumably targets is real and repo-wide (`docs/standards/RELEASE_MANIFEST.md`, `docs/architecture/release-model.md`, `docs/architecture/release-discipline.md` §13.4–13.5, `contracts/release/cosign_attestation_verification_plan.md`), and its shared, non-domain-nested path (`tools/validators/validate_attestation_ref.py`, not under `tools/validators/domains/hazards/`) is itself consistent with those cross-cutting, ReleaseManifest-level documents rather than with hazards specifically — but none of them proposes this exact filename or path either, so no document currently grounds this placeholder's existence at this path under any name. Impact: the placeholder's own citation cannot be used to justify implementing it in place, and its correct owning document/root is unresolved (candidates include a shared release-authority document, not `docs/domains/hazards/`). Resolution path: identify (or write) the document that actually proposes an `attestation_refs[]` verifier, correct or add the citation, and confirm the path against that document's proposal before implementing; until then this citation must not be read as an accepted placement.

- 2026-09-26: CONFIRMED placement conflict — `policy/genealogy/publication.rego` vs. `policy/domains/people-dna-land/`, the policy-root sibling of the already-logged `tools/validators/genealogy/` conflict above. The four-line `PROPOSED` scaffold's own header cites `docs/domains/people-dna-land/sublanes/dna.md` as its source; that document's own domain-lane table (line 381) names `policy/domains/people-dna-land/` as the "(PROPOSED — §12 lane form)" home, not a flat `policy/genealogy/` segment. `policy/domains/people-dna-land/` already carries six real `.rego` files (`abstain_on_ambiguous.rego`, `consent_validator.rego`, `deny_unpublished.rego`, `dna_restricted.rego`, `living_person.rego`, `living_person_redaction.rego`, plus `consent/dna_consent_revocation.rego`) and an already-reserved, still-empty `policy/domains/people-dna-land/genealogy/.gitkeep` subdirectory, while `policy/genealogy/` holds only its own README and this one scaffold. `policy/genealogy/README.md` (v0.2, 561 lines) already self-labels its own status `placement-conflicted` and its `truth_posture` field explicitly names "CONFLICTED policy/genealogy/ compatibility path versus policy/domains/people-dna-land/ domain placement," but the README does not instruct logging here and no DRIFT_REGISTER entry existed until now. Impact: same shape as the tools/validators sibling — implementing `publication.rego` in place would ratify an already-self-flagged-conflicted path over the domain doc's own named alternative and the already-reserved `genealogy/` subdirectory. Resolution path: `ADR-0043` (raised alongside this entry) proposes resolving this the same way `ADR-0042` resolved the tools/validators case.

- 2026-09-26: CONFIRMED stale placeholder under an explicitly retired name — `policy/domains/fauna/api_envelope.rego` is a live four-line `PROPOSED` scaffold, but `docs/domains/fauna/MISSING_OR_PLANNED_FILES.md` (line 368, reconciliation log line 613) states this exact file/construct "was `api_envelope.rego` / `FaunaDecisionEnvelope` — reconciled to `RuntimeResponseEnvelope` / `runtime_envelope.rego`... bespoke envelope retired," naming `policy/domains/fauna/runtime_envelope.rego` as the replacement. `policy/domains/fauna/runtime_envelope.rego` does not exist in the repository at this snapshot — only the doc-declared-retired `api_envelope.rego` does. Impact: a future implementer following the doc's own current table would build `runtime_envelope.rego`, but a naive read of the repository tree would find and could extend the already-retired `api_envelope.rego` instead, reviving a name the domain's own reconciliation note says is gone. Resolution path: either rename the existing scaffold to `runtime_envelope.rego` per the doc's own reconciliation (a mechanical, already-decided rename, not a new placement decision) or delete it and create `runtime_envelope.rego` fresh; either way, do not implement real logic under the retired `api_envelope.rego` name.

- 2026-09-26: CONFIRMED cross-domain placement inconsistency — `policy/domains/<domain>/` vs. `policy/sensitivity/<domain>/` for domain sensitivity-policy scaffolds, confirmed in three domains with inconsistent direction, none previously logged here. (1) Archaeology: `docs/domains/archaeology/MISSING_OR_PLANNED_FILES.md` proposes `policy/sensitivity/archaeology/looting_risk_deny.rego`, `collection_security_deny.rego`, `sacred_site_deny.rego`, and `exact_location_deny.rego` (lines 365-369), but the actual four-line `PROPOSED` scaffolds for all four (plus `burial_and_human_remains_deny.rego`) exist at `policy/domains/archaeology/` instead; `policy/sensitivity/archaeology/` currently holds only `sovereignty_chip_required.rego` and no README, a state `policy/domains/archaeology/sensitivity/README.md` already independently confirmed ("`policy/sensitivity/archaeology/README.md` was not found at the checked path"). (2) Flora: `docs/domains/flora/FILE_SYSTEM_PLAN.md` proposes `policy/domains/flora/rare_plant_geoprivacy.rego`, `join_induced_sensitivity.rego`, `cultural_sensitivity.rego`, and `exact_geometry_deny.rego` (lines 759-762), but the actual four-line scaffolds exist at `policy/sensitivity/flora/` instead — the opposite direction from the archaeology case. `policy/domains/flora/README.md` (line 99) separately states the two lanes should "compose independent controls; do not collapse sensitivity into general domain policy," and already carries its own open, unresolved item on this exact tension (`FLORAPOL-003`, "CONFLICTED / NEEDS ADR OR MIGRATION"), so the FILE_SYSTEM_PLAN.md table may itself be the stale side here rather than the actual file placement. (3) People/DNA/Land: `docs/domains/people-dna-land/DATA_LIFECYCLE.md` (line 552) names `policy/sensitivity/people/living_person.rego` (PROPOSED) as settling living-person policy specifics, but the actual four-line scaffold is `policy/domains/people-dna-land/living_person.rego`; neither `policy/sensitivity/people/` nor `policy/sensitivity/people-dna-land/living_person.rego` exists, though a reserved, empty `policy/sensitivity/people-dna-land/living_person/.gitkeep` directory does, suggesting a third candidate shape (a subdirectory, not a flat file) nobody has chosen either. Impact: at least three domains disagree, in different directions, about which root owns generated sensitivity-policy scaffolds, and Flora's own README already treats this as a live open question without an accepted answer; implementing any of these nine pending scaffolds in place risks the same silent-ratification problem as the other entries here. Resolution path: this is broader than a single-file placement decision and needs its own cross-domain ADR (or an amendment to `ADR-0029`/Directory Rules §12) settling whether `policy/sensitivity/<domain>/` is a distinct, composable lane (as Flora's README already asserts) or should collapse into `policy/domains/<domain>/`, and reconciling each domain's planning doc to match; not attempted in this entry.

- 2026-09-26: CONFIRMED duplicate `SourceDescriptor` placeholders under competing naming conventions — `data/registry/sources/<domain>/` in Agriculture and Hydrology (and one instance in Habitat) each carry multiple auto-generated `PROPOSED` placeholder files for the *same* real-world source, because two sibling planning docs in the same domain independently proposed source-descriptor filenames without cross-referencing each other, and both proposals were separately generated into the repository. (1) Agriculture: `docs/domains/agriculture/FILE_SYSTEM_PLAN.md` names dash-case files (`kansas-mesonet.yaml`, `nasa-hls.yaml`, `nasa-smap.yaml`, `usda-nass.yaml`, each citing `source_doc: docs/domains/agriculture/FILE_SYSTEM_PLAN.md`); `docs/domains/agriculture/VERIFICATION_BACKLOG.md` (lines 216-217, 237-239) independently names underscore-case files for the identical sources (`ks_mesonet.yaml`, `nasa_hls.yaml`, `nasa_smap.yaml`, each citing `source_docs: [docs/domains/agriculture/VERIFICATION_BACKLOG.md]`) — these three are clean one-to-one duplicate pairs. USDA NASS is a distinct, messier case, not a clean pair: `FILE_SYSTEM_PLAN.md:293` names one broad `usda-nass.yaml` covering the whole NASS family (CDL, QuickStats, and Crop Progress together), while `VERIFICATION_BACKLOG.md:216-217` separately proposes `nass_quickstats.yaml` and `nass_crop_progress.yaml` at finer per-product granularity — a one-to-two identity/granularity conflict, not a same-shape duplicate, since the two split files may encode a real distinction the one broad file elides. All nine Agriculture files (four dash/underscore pairs' worth, minus the shared USDA-NASS row, plus the two NASS-split files) exist side by side in `data/registry/sources/agriculture/` today, and neither doc mentions the other's proposal or the resulting overlap. `FILE_SYSTEM_PLAN.md` §11 already self-flags a related but distinct question as unresolved (`V-06`/`OQ-AG-FS-05`: whether `data/registry/sources/agriculture/` is domain-segmented or source-keyed, `CONFLICTED → ADR`) — that is a directory-topology question, not the leaf-filename duplication logged here, and per `data/registry/sources/README.md`'s own write rule ("do not... infer a canonical `data/registry/sources/<domain>/<source_id>` shape from the current tree" until an accepted topology decision exists), neither question should be read as settling the other. Neither was the leaf-naming duplication itself ever logged before now. (2) Hydrology: `docs/domains/hydrology/CANONICAL_PATHS.md` §4.10 names plain files (`usgs_wbd.yaml`, `usgs_nhdplus_hr.yaml`, both present); `docs/domains/hydrology/VERIFICATION_BACKLOG.md` (`HYD-VB-001`, `HYD-VB-002`) independently proposes `.source.yaml`-suffixed files for the identical sources (`wbd.source.yaml`, `nhdplus_hr.source.yaml`), and both are also present in `data/registry/sources/hydrology/` today — clean one-to-one pairs. (3) Habitat: `docs/domains/habitat/CANONICAL_PATHS.md` names the merged "USFWS ECOS / critical habitat services" source `usfws_ecos.yaml` (present); `docs/domains/habitat/FILE_SYSTEM_PLAN.md`'s tree (line 301 area) independently proposes the entire `data/registry/sources/habitat/` set under a `<name>.source.yaml`, dash-case convention instead (`nlcd.source.yaml`, `gap-landfire.source.yaml`, `pad-us.source.yaml`, etc.) — only one of those competing names, `usfws-critical-habitat.source.yaml`, was actually generated, and it now sits alongside `usfws_ecos.yaml` for what both docs describe as the same source; the other seven Habitat files follow `CANONICAL_PATHS.md`'s convention only, so the duplication here is a single inconsistent outlier rather than a fully doubled set. Impact: at least five real-world sources across two domains (Kansas Mesonet, NASA HLS, NASA SMAP, USGS WBD, NHDPlus HR) currently have two parallel `PROPOSED` `SourceDescriptor` placeholders apiece; USDA NASS separately has three placeholders spanning two identity granularities; and a third domain (Habitat) has one stray outlier — exactly the "two homes disagreeing about the same object family" risk this repository's own review controls are meant to catch before it reaches real rights/terms/cadence content; whichever file an implementer fills in first would silently orphan its sibling(s). Resolution path: this needs a steward decision (and likely a cross-domain ADR) on the leaf-naming duplication specifically — this entry does not propose or assume an outcome, does not equate it with the separate domain-segmented-vs-source-keyed topology question already tracked by `V-06`/`OQ-AG-FS-05`, and does not recommend deleting either sibling: `data/registry/sources/README.md`'s own write rule already prohibits silently migrating, mirroring, or deleting records or inferring a canonical shape while topology is unresolved, so any reconciliation needs an accepted decision first, preserving both identities as inactive evidence in the meantime (the same posture `pipeline_specs/`'s own self-declared `HOLD`s use); USDA NASS additionally needs its one-to-two identity question resolved before any file count claim is made at all. Not attempted in this entry.

- 2026-09-26: CONFIRMED three-way casing triplication of 15 schema concepts in `schemas/contracts/v1/domains/atmosphere/` — the *same* canonical, domain-segmented directory (not a cross-root split like the other entries here) holds three separately-generated `PROPOSED` scaffold files per concept, one in each of three casing conventions, because three sibling Atmosphere planning docs each independently proposed the domain's schema inventory under a different filename style and none cross-referenced the others: `docs/domains/atmosphere/CANONICAL_PATHS.md` → PascalCase (e.g. `AirStation.schema.json`, `AODRaster.schema.json`), `docs/domains/atmosphere/FILE_SYSTEM_PLAN.md` → kebab-case (`air-station.schema.json`, `aod-raster.schema.json`), `docs/domains/atmosphere/MISSING_OR_PLANNED_FILES.md` → snake_case (`air_station.schema.json`, `aod_raster.schema.json`) — confirmed by direct inspection of each file's own `x-kfm.source_doc`/`source_docs` field, which maps consistently to its casing style. All fifteen affected concepts, each present in all three castings (45 files total, 30 of them excess): `AdvisoryContext`, `AirObservation`, `AirStation`, `AODRaster`, `ClimateAnomaly`, `ClimateNormal`, `ForecastContext`, `OzoneObservation`, `PM25Observation`, `PrecipitationObservation`, `SmokeContext`, `TemperatureObservation`, `WeatherObservation`, `WeatherStation`, `WindField`. A repository-wide scan of every other populated `schemas/contracts/v1/domains/<domain>/` directory (Agriculture 24, Archaeology 40, Fauna 34, Flora 38, Geology 40, Habitat 30, Hazards 23, Hydrology 42, People/DNA/Land 20, Roads/Rail/Trade 20, Settlements/Infrastructure 18, Soil 38, Water Planning 18 files) found this three-way triplication nowhere else except one isolated two-way (`evidence-bundle.schema.json` / `evidence_bundle.schema.json`) in Habitat — this is otherwise an Atmosphere-specific incident, not a repository-wide generation defect. The unaffected "clean" domains (Soil, Hydrology, and the rest) consistently use snake_case filenames throughout, which is evidence for — but not proof of — snake_case being the de facto repository convention that Atmosphere's PascalCase and kebab-case variants deviate from; this entry does not assert that as accepted. This exact `AirStation` concept also has an eighth, unrelated-root copy already logged in the singular-`settlement`-style compatibility placeholder at `schemas/contracts/v1/air/AirStation.schema.json` (`docs/domains/atmosphere/EXPANSION_PLAN.md`-sourced) — a fourth citing document and a fourth casing-and-location combination for the same real-world concept, not counted in the 45/30 figures above. Impact: an implementer defining real fields for any of these 15 concepts has three (or, for AirStation, four) plausible files to choose from with no signal in the repository for which is authoritative, risking exactly the same "two homes disagreeing about the same object family" outcome already logged for other findings in this register, but concentrated within a single canonical directory rather than spread across roots. Resolution path: not attempted here. This needs a steward decision (most plausibly a validator rule plus a reviewed cleanup PR, since — unlike the cross-root cases in this register — there is no compatibility-alias precedent needed here, only a same-directory naming-convention pick) settling one canonical casing style for `schemas/contracts/v1/domains/<domain>/` filenames repository-wide, after which the two non-canonical files per concept in Atmosphere are retired (preserving any unique field content, though all fifteen currently share the identical empty `PROPOSED scaffold` body, so no content-loss risk currently exists); the `schemas/contracts/v1/air/AirStation.schema.json` outlier is a separate, already-logged category of finding and should be resolved alongside the broader `settlement`-style singular-root question, not folded into this cleanup.

- 2026-09-26: CONFIRMED self-flagged placement HOLDs in `pipeline_specs/` never carried into this register — two `KfmPipelineSpecDeclaration` placement conflicts (competing declarations for the same concept) plus one boundary-placement question with no declaration on either side yet are already explicitly disclosed as `HOLD` with reason code `PLACEMENT_REVIEW_REQUIRED` inside their own owning READMEs, each stating it does not assert a canonical target, but none was ever logged here despite this register's role as the append-only conflict log the rest of this audit series has consistently used for exactly this shape of finding. (1) Hydrology: `pipeline_specs/hydrology/ingest_nhdplus_hr.yaml` (citing `docs/domains/hydrology/CANONICAL_PATHS.md`) and `pipeline_specs/hydrology/nhdplus_hr_ingest.yaml` (citing `docs/domains/hydrology/FILE_SYSTEM_PLAN.md`) are two separate `PIPELINE_CANDIDATE`/`INGEST` declarations for the identical NHDPlus HR ingest concept, both `PROPOSED_INACTIVE`/`NOT_IMPLEMENTED`, both carrying `PLACEMENT_REVIEW_REQUIRED` in their own `reason_codes`; `pipeline_specs/hydrology/README.md`'s own status table and "Status and open verification" section (`HOLD / PLACEMENT_REVIEW_REQUIRED: resolve ingest_nhdplus_hr.yaml versus nhdplus_hr_ingest.yaml without inventing a canonical target here`) already documents this exact pair, in contrast to the structurally identical WBD case in the same directory, which the README shows as already reconciled (`ingest_wbd.yaml` is a declared `COMPATIBILITY_ALIAS` with `canonical_target: pipeline_specs/hydrology/wbd_huc12_ingest.yaml`). (2) Flora/Watchers: `pipeline_specs/flora/plants_drift_watcher.yaml` and `pipeline_specs/watchers/plants_drift.yaml` are two separate `PIPELINE_CANDIDATE`/`WATCH` declarations for the identical plants-drift-watcher concept (both cite `docs/domains/flora/FILE_SYSTEM_PLAN.md`, both `PROPOSED_INACTIVE`/`NOT_IMPLEMENTED`); `pipeline_specs/flora/README.md` ("`HOLD / PLACEMENT_REVIEW_REQUIRED`: decide whether `plants_drift_watcher.yaml`, `watchers/`, or the shared `pipeline_specs/watchers/` lane owns watcher intent. No canonical target is asserted here.") and `pipeline_specs/watchers/README.md` ("Its outcome is `HOLD` with reason `PLACEMENT_REVIEW_REQUIRED`. This README does not assert which path is canonical...") both independently confirm the same open conflict from each side. (3) Fauna/Watchers — a boundary-placement question, not a declaration conflict: `pipeline_specs/fauna/watchers/README.md`'s own "Declaration inventory" table reads `None | None | None | HOLD / PLACEMENT_REVIEW_REQUIRED`, and its `pipeline_specs/fauna/README.md` parent separately states "The nested `watchers/` placement is `HOLD / PLACEMENT_REVIEW_REQUIRED`; this contract does not invent a canonical target or imply that a Fauna watcher profile exists" — the still-empty `pipeline_specs/fauna/watchers/` (README + `.gitkeep` only) has no declaration file on either side to conflict with the shared `pipeline_specs/watchers/` lane, unlike the Hydrology and Flora pairs above; a future declaration here first needs a separate reviewed placement decision before any YAML is authored, per the README's own text. Impact: none of these three self-disclosed HOLDs is a new discovery — each owning document already does exactly what this repository's governance model asks (flag the conflict or open question, hold, name no canonical target) — but their absence from this register means the audit trail this register exists to provide is incomplete, and a future reviewer scanning only `DRIFT_REGISTER.md` would miss all three. Resolution path: no new decision is proposed by this entry; it only carries each already-self-declared `HOLD` into the append-only log per this register's own stated purpose. The Hydrology and Flora/Watchers declaration conflicts should be resolved (or explicitly deferred) the same way the WBD case already was — a compatibility alias plus `canonical_target`, or an ADR, once a steward decides; the Fauna/Watchers question is narrower and needs only a placement decision (Fauna's own nested `watchers/` versus the shared `pipeline_specs/watchers/` lane) before any declaration is written there, not a compatibility alias between two files that do not yet exist.

- 2026-09-26: CONFIRMED extensively self-flagged singular-`settlement`-vs-`settlements-infrastructure` conflict never carried into this register — `schemas/contracts/v1/settlement/` (the flat, singular schema root) holds three real `PROPOSED` scaffolds (`dependency.schema.json`, `fort_event.schema.json`, `service_area.schema.json`), each citing `docs/domains/settlements-infrastructure/EXPANSION_BACKLOG.md` as its source. That source document (§ SET-BL-0006, citing "Atlas Ch. 24.13") does instruct the singular `schemas/contracts/v1/settlement/` path for these schemas, so the citation itself is internally consistent — but the domain's actual populated, self-declared-canonical schema home has since moved to `schemas/contracts/v1/domains/settlements-infrastructure/`, which already holds eighteen real schema files (`domain_feature_identity.schema.json`, `evidence_bundle.schema.json`, `release_manifest.schema.json`, `rollback_card.schema.json`, and fourteen others) following the domain-segmented convention used by every other domain in this repository (`schemas/contracts/v1/domains/soil/`, `.../hydrology/`, `.../habitat/`, etc.). `schemas/contracts/v1/settlement/README.md` already extensively self-documents exactly this tension — a 0.1-version, multi-owner "Settlement Schema Compatibility Index" whose own `status` field reads `singular-settlement-compatibility-path; settlements-infrastructure-overlap; schema-scaffolds-present; ...CONFLICTED...`, and whose notes explicitly state "`schemas/contracts/v1/domains/settlements-infrastructure/README.md` identifies itself as the ... canonical [home] ... while `schemas/contracts/v1/settlement/README.md` documents the flat singular settlement lane as compatibility posture" and warns this "must not become parallel canonical schema authority without ADR or migration review." No such ADR exists, and — despite this being one of the most thoroughly self-documented conflicts found in this entire audit series — no `DRIFT_REGISTER.md` entry existed before now. The same singular `settlement/` root recurs, per the guardrail README's own cross-references and direct repository inspection, as a parallel, mostly README-or-`.gitkeep`-only compatibility placeholder alongside the canonical `settlements-infrastructure/` (or `domains/settlements-infrastructure/`) name across at least `packages/domains/`, `pipelines/domains/`, `pipeline_specs/`, `fixtures/domains/` (and `fixtures/public_safe/`), `tests/domains/`, `contracts/` (both `contracts/settlement/` and `contracts/domains/settlement/`), `release/candidates/`, `policy/sensitivity/`, `policy/release/`, and multiple `data/<lifecycle-stage>/` roots (`raw`, `work`, `quarantine`, `processed`, `catalog/domain`, `published/layers`, `proofs`, `receipts`, `triplets`) — not exhaustively re-verified content-by-content in this entry beyond the schemas/ case above, but each root's own `settlement/README.md` (where present) already names the same overlap. Impact: this is a repository-wide naming-compatibility question of the same shape as a different pair of roots the repository has partially addressed before (`ADR-0002`, `status: draft`, and `ADR-0003`, `status: proposed`, "`policy/` (singular) is canonical; `policies/` is compatibility") — but that precedent itself is only partly settled: accepted `ADR-0029` adopts Directory Rules bytes placing singular `policy/` as the writable root, while `ADR-0003`'s own text states the narrower "`policies/` is compatibility" migration contract "remains proposed" and is not accepted merely because the placement foundation is. So the available precedent is a template for the *shape* of a resolving ADR, not a settled analogous case to point to as already-resolved. For `settlement`/`settlements-infrastructure` the question remains fully open, self-disclosed in dozens of guardrail documents, and absent from the one register meant to make such disclosures discoverable in one place. Resolution path: not attempted here. This needs a steward decision (most plausibly a new ADR following the same two-step shape as `ADR-0029`/`ADR-0003` — an accepted root-placement decision, then a separate compatibility/migration contract) confirming `schemas/contracts/v1/domains/settlements-infrastructure/` — and, by the same logic, the domain-segmented `settlements-infrastructure/` form generally — as canonical, then a reviewed migration retiring the three stale `schemas/contracts/v1/settlement/` scaffolds (preserving their content per the `EXPANSION_BACKLOG.md` proposal) and, separately, an inventory of whether each of the other singular-`settlement/` roots listed above needs the same treatment or already serves a distinct, legitimate purpose.

- 2026-09-26: CONFIRMED corrections to the Atmosphere schema-casing-triplication entry above (three errors, one significant) — logged as a new entry per this register's append-only convention rather than editing the merged entry in place. (1) **Significant — not all fifteen concepts are interchangeable empty scaffolds.** `AirObservation` and `ForecastContext` are exceptions the original entry's blanket "all fifteen currently share the identical empty `PROPOSED scaffold` body" claim missed: `schemas/contracts/v1/domains/atmosphere/README.md`'s own schema-inventory table lists `air_observation.schema.json` and `forecast_context.schema.json` (snake_case) as substantive `DRAFT_SCHEMA` bounded profiles with real anti-collapse rules, fixtures, and a bound validator, and lists `AirObservation.schema.json` / `ForecastContext.schema.json` (PascalCase) as intentional one-way `MIRROR`s (`$ref` pointers, `x-kfm.status: MIRROR`) that `tests/domains/atmosphere/test_observed_modeled_separation.py::test_camelcase_schema_paths_are_one_way_mirrors` actively enforces. Only the kebab-case `air-observation.schema.json` / `forecast-context.schema.json` are the genuinely stale, empty `PROPOSED` scaffolds for these two concepts. A steward or cleanup PR following the original entry's "retire the two non-canonical files per concept" resolution path verbatim would delete a tested compatibility mirror and an implemented canonical schema for these two concepts specifically; the remaining thirteen concepts are unaffected by this correction and remain three-way empty-scaffold duplicates as originally described. (2) The entry called the `schemas/contracts/v1/air/AirStation.schema.json` copy an "eighth" copy in one clause while correctly calling it a "fourth" combination in the very next clause; the correct count throughout is four (three within `schemas/contracts/v1/domains/atmosphere/` plus this one), not eight. (3) The entry called this same `air/AirStation.schema.json` copy "already logged" via the singular-`settlement`-style entry from this same day — that entry documents only the `settlement`/`settlements-infrastructure` conflict and never names `AirStation` or `schemas/contracts/v1/air/` at all, so no prior entry in fact covers this specific file; treat it as newly noted here, not previously logged. (4) Separately, the claim that "unaffected" domains "consistently use snake_case filenames throughout" is stated too strongly: `schemas/contracts/v1/domains/hydrology/hydro-crosswalk-manifest.schema.json`, `.../habitat/evidence-bundle.schema.json`, `.../habitat/habitat-fauna-join.schema.json`, `.../roads-rail-trade/continuity-inventory.schema.json`, `.../roads-rail-trade/transport-facility.schema.json`, and `.../settlements-infrastructure/place-identity.schema.json` are all kebab-case exceptions within otherwise-snake_case domains; snake_case is predominant across the repository, not universal, and this weakens (without eliminating) it as evidence for a future canonical-casing decision. Resolution path: any future steward decision or validator rule addressing the Atmosphere triplication must treat `AirObservation` and `ForecastContext` as a two-file problem (retire only the kebab-case scaffold, leave the snake_case `DRAFT_SCHEMA` and PascalCase `MIRROR` both in place) rather than a three-file problem like the other thirteen concepts, and must not cite either the corrected "eighth copy" figure or the false "already logged" claim as prior art.

- 2026-09-26: CONFIRMED self-flagged singular-`source/`-vs-plural-`sources/` HOLD, plus three additional stale scaffolds within `source/` under mis-cited ADR-0001 authority, never carried into this register — a different object and directory than the `data/registry/sources/` `SourceDescriptor` duplication logged above; this is the `SourceDescriptor` **schema** family at `schemas/contracts/v1/source(s)/`. `schemas/contracts/v1/source/source_descriptor.schema.json` is the real, substantive, field-rich implementation (35,859 bytes), but its own `x-kfm` block names `schemas/contracts/v1/sources/source_descriptor.schema.json` as `canonical_schema_path` and itself as `legacy_schema_path`; `schemas/contracts/v1/sources/source_descriptor.schema.json` is a four-line `$ref` compatibility alias back to it, explicitly documented as adding "no independent source-admission, rights, policy, review, release, or publication authority." Both `README.md` files already self-document this as unresolved: `schemas/contracts/v1/source/README.md`'s `truth_posture` field names "HOLD singular-plural and hyphen-underscore disposition" and its own Q&A table states "Are hyphen/underscore names resolved? No. A rich underscore schema and permissive hyphen scaffold coexist," while `schemas/contracts/v1/sources/README.md` (`status: draft; ...; NEEDS VERIFICATION before promotion`) independently asks in its own "Open questions" table, "Should the canonical path be `source/` or `sources/`?" — answered only "NEEDS VERIFICATION / migration-sensitive." Layered on top: within singular `source/`, three additional empty `PROPOSED` scaffolds sit beside the one substantive implementation — `source-descriptor.schema.json` (562 bytes, `properties: {}`), `source-descriptor.json` (1012 bytes, `properties: {}`), and `source_descriptor.json` (256 bytes, `properties: {}`) — each citing a different domain doc that independently asserts "per ADR-0001" for its own distinct filename/casing choice: `docs/domains/archaeology/SOURCE_REGISTRY.md` (lines 232-233, 307, 909) names `source-descriptor.json` (dash, no `.schema` infix); `docs/domains/agriculture/SOURCE_REGISTRY.md` (lines 134, 177, 302, 390) names `source-descriptor.schema.json` (dash, with `.schema`); `docs/domains/archaeology/ARCHITECTURE.md` (line 379) names `source_descriptor.schema.json` (underscore, with `.schema`) — three mutually exclusive filenames, each "per ADR-0001." Direct inspection of `docs/adr/ADR-0001-schema-home--schemas-contracts-v1-is-canonical.md` Appendix A confirms it commits only to the **directory** `schemas/contracts/v1/source/` for "Source object[s]" and never addresses leaf filename casing or the `.schema` suffix, so each citing document's "per ADR-0001" claim is directionally correct (right directory) but overstates what the ADR actually settled. No `DRIFT_REGISTER.md` entry previously named `schemas/contracts/v1/source/`, `schemas/contracts/v1/sources/`, `source-descriptor`, or `source_descriptor` in this schema sense. Impact: an implementer choosing among four scaffolds at `source/` (one substantive, three empty) plus the question of whether `source/` or `sources/` is even the right root has no register signal, only the two README's own already-thorough but siloed self-documentation. Resolution path: not attempted here. This needs a steward/ADR decision on (a) `source/` vs. `sources/` placement, per both READMEs' own promotion checklists, which already enumerate this as an unmet prerequisite, and (b) which of the three empty-scaffold filenames (if any) should be retired once (a) is settled, correcting each citing document's overstated "per ADR-0001" claim about filename casing in the process.

- 2026-09-26: CONFIRMED correction to the source/sources SourceDescriptor schema HOLD entry above — logged as a new entry per this register's append-only convention rather than editing the merged entry in place, since the underlying finding (`source/`-vs-`sources/` placement) is unaffected. The original entry's "three additional empty `PROPOSED` scaffolds" characterization conflates two different kinds of file. Only `schemas/contracts/v1/source/source-descriptor.schema.json` is an empty JSON Schema (`type: object`, `properties: {}`, `additionalProperties: true`). `schemas/contracts/v1/source/source-descriptor.json` and `schemas/contracts/v1/source/source_descriptor.json` are not JSON Schema documents at all — they carry only `status`, `path`, `source_docs`, and `notes` fields, with no `$schema`, `type`, or `properties` key. `schemas/contracts/v1/source/README.md`'s own file-inventory table (lines 102-103) already says so explicitly for both: "Proposal metadata record, not JSON Schema" / "Do not validate payloads against it." The original entry's framing — "an implementer choosing among four scaffolds at `source/` (one substantive, three empty)" and a resolution path asking "which of the three empty-scaffold filenames (if any) should be retired" — therefore overstates the schema conflict: there is one substantive schema, one empty-scaffold schema competing with it, and two non-schema proposal-metadata records that a schema-retirement decision does not need to treat as schema candidates at all. The three-way "per ADR-0001" filename citation conflict among `docs/domains/archaeology/SOURCE_REGISTRY.md`, `docs/domains/agriculture/SOURCE_REGISTRY.md`, and `docs/domains/archaeology/ARCHITECTURE.md` is unaffected by this correction — all three still name one of these three files, regardless of which are schemas versus metadata records. Resolution path: any future steward decision on the empty-scaffold-versus-implementation question should weigh `source-descriptor.schema.json` against `source_descriptor.schema.json` only; the two `.json` metadata records are a separate, lower-stakes question (whether a proposal-metadata record citing a since-superseded filename convention should be retired or updated), not a three-way schema retirement.
