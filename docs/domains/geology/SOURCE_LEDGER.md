# Source Ledger

**Status:** PROPOSED scaffold.
**Source:** `docs/domains/geology/FILE_SYSTEM_PLAN.md`

This file was created because the domain markdown inventory lists `docs/domains/geology/SOURCE_LEDGER.md` as a planned or expected path. Fill in the authoritative content, owners, validation status, and links before promoting it beyond scaffold status.

## Notes

- Keep machine-checkable shape in `schemas/`, policy in `policy/`, fixtures in `fixtures/`, and release decisions in `release/` unless an accepted ADR says otherwise.
- Replace this scaffold with domain-reviewed content before treating the path as canonical truth.
